import PitchPilot from '../../../../screens/app/pitch/PitchPilot'

/** Pitch Pilot owns its own left rail, so one segment serves all its sections. */
export default function Page() {
  return <PitchPilot />
}
