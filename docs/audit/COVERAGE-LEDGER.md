# Source and interaction coverage ledger

Generated by `npm run audit:inventory`. Every runtime source file is hashed and parsed; this is static inventory, **not proof of browser testing or business completeness**. Source-inventory JSON retains full data declarations, field schemas, event-handler attributes, local state and effect/callback bodies. Dynamic mapped controls must be expanded using their referenced data declarations. The implementing agent must assign each interaction a backend operation, permission, state requirements, acceptance test and evidence; UNVERIFIED may never be silently promoted to complete.

Files: 173; route files: 49; control call sites: 739.

| File | Lines | Controls | Links | Declarations |
|---|---:|---:|---:|---|
| [src/app/[...slug]/page.tsx](../../src/app/[...slug]/page.tsx) | 15 | 0 | 0 | generateStaticParams, Page |
| [src/app/app/[slug]/page.tsx](../../src/app/app/[slug]/page.tsx) | 10 | 0 | 0 | Page |
| [src/app/app/account/page.tsx](../../src/app/app/account/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/admin/scheduled-jobs/page.tsx](../../src/app/app/admin/scheduled-jobs/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/agent-portal/page.tsx](../../src/app/app/agent-portal/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/approvals/page.tsx](../../src/app/app/approvals/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/asset-management/page.tsx](../../src/app/app/asset-management/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/build/agent-studio/page.tsx](../../src/app/app/build/agent-studio/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/build/vibe-studio/page.tsx](../../src/app/app/build/vibe-studio/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/business/chat/page.tsx](../../src/app/app/business/chat/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/business/page.tsx](../../src/app/app/business/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/business/tool/[slug]/page.tsx](../../src/app/app/business/tool/[slug]/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/client-portal/page.tsx](../../src/app/app/client-portal/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/compliance-center/page.tsx](../../src/app/app/compliance-center/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/crm/page.tsx](../../src/app/app/crm/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/governance/page.tsx](../../src/app/app/governance/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/guardrails/page.tsx](../../src/app/app/guardrails/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/hr/page.tsx](../../src/app/app/hr/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/inbox/page.tsx](../../src/app/app/inbox/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/integrations/page.tsx](../../src/app/app/integrations/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/layout.tsx](../../src/app/app/layout.tsx) | 22 | 0 | 0 | AppLayout |
| [src/app/app/marketplace/page.tsx](../../src/app/app/marketplace/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/announcements/page.tsx](../../src/app/app/me/announcements/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/attendance/page.tsx](../../src/app/app/me/attendance/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/documents/page.tsx](../../src/app/app/me/documents/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/layout.tsx](../../src/app/app/me/layout.tsx) | 8 | 0 | 0 | MeLayout |
| [src/app/app/me/leaves/page.tsx](../../src/app/app/me/leaves/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/onboarding/page.tsx](../../src/app/app/me/onboarding/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/page.tsx](../../src/app/app/me/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/payslips/page.tsx](../../src/app/app/me/payslips/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/profile/page.tsx](../../src/app/app/me/profile/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/team-approvals/page.tsx](../../src/app/app/me/team-approvals/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/me/timesheets/page.tsx](../../src/app/app/me/timesheets/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/page.tsx](../../src/app/app/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/pitch-pilot/[[...section]]/page.tsx](../../src/app/app/pitch-pilot/[[...section]]/page.tsx) | 7 | 0 | 0 | Page |
| [src/app/app/prompt-lab/page.tsx](../../src/app/app/prompt-lab/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/runners/page.tsx](../../src/app/app/runners/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/runs/page.tsx](../../src/app/app/runs/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/sales-pos/page.tsx](../../src/app/app/sales-pos/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/scheduled-jobs/page.tsx](../../src/app/app/scheduled-jobs/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/setup/companies/page.tsx](../../src/app/app/setup/companies/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/setup/page.tsx](../../src/app/app/setup/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/support-ticketing/page.tsx](../../src/app/app/support-ticketing/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/app/travel-expense/page.tsx](../../src/app/app/travel-expense/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/globals.css](../../src/app/globals.css) | 832 | 0 | 0 |  |
| [src/app/layout.tsx](../../src/app/layout.tsx) | 35 | 0 | 3 | metadata, RootLayout |
| [src/app/login/page.tsx](../../src/app/login/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/marketplace/page.tsx](../../src/app/marketplace/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/not-found.tsx](../../src/app/not-found.tsx) | 13 | 0 | 1 | NotFound |
| [src/app/onboarding/page.tsx](../../src/app/onboarding/page.tsx) | 14 | 0 | 0 | Page |
| [src/app/page.tsx](../../src/app/page.tsx) | 6 | 0 | 0 | Page |
| [src/app/providers.tsx](../../src/app/providers.tsx) | 18 | 0 | 0 | Providers |
| [src/app/register/page.tsx](../../src/app/register/page.tsx) | 11 | 0 | 0 | Page |
| [src/app/studio/page.tsx](../../src/app/studio/page.tsx) | 11 | 0 | 0 | Page |
| [src/app/studio/workspace/[workspaceId]/home/page.tsx](../../src/app/studio/workspace/[workspaceId]/home/page.tsx) | 11 | 0 | 0 | Page |
| [src/components/AppChrome.tsx](../../src/components/AppChrome.tsx) | 230 | 6 | 0 | ChromeTab, AppChrome, Toolbar, Chip, Select, SearchBox, Segmented, Action, BigEmpty, Card |
| [src/components/AppShell.tsx](../../src/components/AppShell.tsx) | 67 | 1 | 1 | TopBar, AppShell |
| [src/components/AppSideChrome.tsx](../../src/components/AppSideChrome.tsx) | 148 | 3 | 0 | SideNavItem, SideNavGroup, AppSideChrome, SubTabs |
| [src/components/AppSidebar.tsx](../../src/components/AppSidebar.tsx) | 278 | 2 | 3 | isActive, appIcons, appTones, appRoutes, Item, Group, AppSidebar, enterpriseGroup |
| [src/components/AuthLayout.tsx](../../src/components/AuthLayout.tsx) | 86 | 0 | 2 | Stat, Props, AuthLayout |
| [src/components/CopilotDock.tsx](../../src/components/CopilotDock.tsx) | 103 | 5 | 0 | CopilotDock, ask |
| [src/components/CountUp.tsx](../../src/components/CountUp.tsx) | 126 | 0 | 0 | split, CountUp |
| [src/components/Dialog.tsx](../../src/components/Dialog.tsx) | 44 | 1 | 0 | Dialog, sizes |
| [src/components/EnterpriseUi.tsx](../../src/components/EnterpriseUi.tsx) | 58 | 0 | 0 | EmptyBlock, Panel, Label |
| [src/components/Icon.tsx](../../src/components/Icon.tsx) | 169 | 0 | 0 | IconName, paths, Icon |
| [src/components/NavGroup.tsx](../../src/components/NavGroup.tsx) | 52 | 2 | 0 | NavGroup |
| [src/components/PageHeader.tsx](../../src/components/PageHeader.tsx) | 40 | 0 | 0 | PageHeader, EmptyState |
| [src/components/Pointer.tsx](../../src/components/Pointer.tsx) | 49 | 0 | 0 | Pointer |
| [src/components/PortalLayout.tsx](../../src/components/PortalLayout.tsx) | 62 | 0 | 2 | PortalLayout |
| [src/components/ProductPreview.tsx](../../src/components/ProductPreview.tsx) | 140 | 0 | 0 | ProductPreview, rail, stats, tasks |
| [src/components/RecordDialog.tsx](../../src/components/RecordDialog.tsx) | 214 | 8 | 0 | spanClass, Label, dateFor, initialValue, Field, RecordDialog, fields |
| [src/components/RecordList.tsx](../../src/components/RecordList.tsx) | 18 | 1 | 0 | RecordList |
| [src/components/Reveal.tsx](../../src/components/Reveal.tsx) | 65 | 0 | 0 | Reveal |
| [src/components/ScrollProgress.tsx](../../src/components/ScrollProgress.tsx) | 38 | 0 | 0 | ScrollProgress |
| [src/components/SiteLayout.tsx](../../src/components/SiteLayout.tsx) | 155 | 0 | 11 | platformLinks, SiteHeader, SiteFooter, SiteLayout |
| [src/components/SiteRail.tsx](../../src/components/SiteRail.tsx) | 141 | 2 | 3 | RailItem, railItems, themeOptions, moreLinks, SiteRail, onPointerDown, onKeyDown |
| [src/components/StatCard.tsx](../../src/components/StatCard.tsx) | 26 | 0 | 0 | StatCard, labels, values |
| [src/components/ThemeSwitch.tsx](../../src/components/ThemeSwitch.tsx) | 52 | 1 | 0 | options, ThemeSwitch |
| [src/components/VippyWidget.tsx](../../src/components/VippyWidget.tsx) | 92 | 6 | 0 | VippyWidget, send |
| [src/components/WordReveal.tsx](../../src/components/WordReveal.tsx) | 49 | 0 | 0 | WordReveal, Stagger |
| [src/components/ui.tsx](../../src/components/ui.tsx) | 141 | 4 | 0 | Logo, ButtonProps, buttonStyles, Button, FieldProps, Field, GoogleMark, MicrosoftMark, SsoButtons, Divider, Eyebrow, Pill |
| [src/lib/agentCatalog.ts](../../src/lib/agentCatalog.ts) | 178 | 0 | 0 | CatalogAgent, AgentGroup, agentGroups, groupTones |
| [src/lib/agentStudioData.ts](../../src/lib/agentStudioData.ts) | 128 | 0 | 0 | StudioNavItem, StudioNavGroup, studioPrimary, studioGroups, Workflow, TemplateCategory, templateCategories |
| [src/lib/appData.ts](../../src/lib/appData.ts) | 194 | 0 | 0 | SuiteTool, suiteTools, VibeSkill, vibeSkills, AppKind, MarketApp, apps, SmallApp, webApps, mobileApps, RunStep, sampleTrace, Approval, pendingApprovals, builderTiles, quickTools, jumpCards |
| [src/lib/appNav.ts](../../src/lib/appNav.ts) | 170 | 0 | 0 | NavLeaf, NavGroup, workspaceGroup, buildGroup, runGroup, toolsGroup, administerGroup, analyzeGroup, AppPortal, appPortals |
| [src/lib/assetData.ts](../../src/lib/assetData.ts) | 119 | 0 | 0 | mastersTab, AssetStage, assetSettingsNav, TaxonomyEntry, taxonomyGroups, defaultAssetTypes, defaultModeOfPurchase, defaultRetirementReasons, defaultSupportReasons, requestTypes, requestStatuses, approvalTypes, approverRoles |
| [src/lib/auth.tsx](../../src/lib/auth.tsx) | 135 | 0 | 0 | SignUpInput, AuthValue, read, write, AuthProvider, next, next, useAuth |
| [src/lib/businessData.ts](../../src/lib/businessData.ts) | 253 | 0 | 0 | SuiteTool, SuiteCategory, suiteCategories, toolSlug, LocatedTool, quickAccess, featuredToolNames, chatSuggestions, chatModels |
| [src/lib/complianceData.ts](../../src/lib/complianceData.ts) | 112 | 0 | 0 | InventoryCategory, InventoryField, dataInventory, FlowType, DataFlow, dataFlows, flowTone, legalBases, inventoryCategories |
| [src/lib/connectorData.ts](../../src/lib/connectorData.ts) | 60 | 0 | 0 | ConnectorAuth, Connector, connectors, authLabel |
| [src/lib/content.ts](../../src/lib/content.ts) | 65 | 0 | 0 | suggestionChips, industries, suites, enterpriseApps, trustPoints, signInPoints, teamSizes |
| [src/lib/crmData.ts](../../src/lib/crmData.ts) | 135 | 0 | 0 | Stage, pipelineStages, CrmTabId, leadStatuses, contactTypes, settingsGroups, leadSources, scoreBands, activityTypes, timeRanges |
| [src/lib/csv.ts](../../src/lib/csv.ts) | 46 | 0 | 0 | parseCsv, rows, row, csvCell, recordsCsv, downloadCsv |
| [src/lib/guardrailData.ts](../../src/lib/guardrailData.ts) | 35 | 0 | 0 | GuardrailType, GuardrailAction, CheckPoint, guardrailTypes, guardrailActions, checkPoints, actionTone |
| [src/lib/hrData.ts](../../src/lib/hrData.ts) | 785 | 0 | 0 | HrField, HrModal, HrSubTab, HrStat, LegendEntry, HrPage, HrGroup, hrGroups, HrSetting, hrSettings |
| [src/lib/landingData.ts](../../src/lib/landingData.ts) | 235 | 0 | 0 | suggestionChips, industries, suites, agentStudioCapabilities, PipelineNode, pipelines, integrationLogos, marketplacePicks, trustPillars, whyUs, plans, faqs |
| [src/lib/pages/index.ts](../../src/lib/pages/index.ts) | 10 | 0 | 0 | allPages |
| [src/lib/pages/info.ts](../../src/lib/pages/info.ts) | 256 | 0 | 0 | infoPages |
| [src/lib/pages/moreContent.ts](../../src/lib/pages/moreContent.ts) | 59 | 0 | 0 | moreLinksContent |
| [src/lib/pages/product.ts](../../src/lib/pages/product.ts) | 333 | 0 | 0 | productPages |
| [src/lib/pages/types.ts](../../src/lib/pages/types.ts) | 22 | 0 | 0 | Card, Section, Cta, PageSpec |
| [src/lib/pages/vibeSkills.ts](../../src/lib/pages/vibeSkills.ts) | 201 | 0 | 0 | SkillSpec, skills |
| [src/lib/pitchData.ts](../../src/lib/pitchData.ts) | 94 | 0 | 0 | pitchNav, RfpStage, inFlightStages, verticals, kbCategories, stockSchemas, brandDefaults |
| [src/lib/portalData.ts](../../src/lib/portalData.ts) | 45 | 0 | 0 | PortalTab, portalTabs, scopeHelp |
| [src/lib/posData.ts](../../src/lib/posData.ts) | 141 | 0 | 0 | PosNavItem, posNav, orderStatuses, deliveryStatuses, invoiceStatuses, paymentFilters, sourceFilters, orderToCashStages, agingBuckets, paymentDueBands, posReportTabs, months, toleranceActions, standardCustomerGroups |
| [src/lib/relativeTime.ts](../../src/lib/relativeTime.ts) | 20 | 0 | 0 | relativeTime |
| [src/lib/router.tsx](../../src/lib/router.tsx) | 119 | 0 | 1 | LinkProps, Link, useParams, flat, useLocation, useNavigate, ParamInit, useSearchParams, Navigate |
| [src/lib/supportData.ts](../../src/lib/supportData.ts) | 167 | 0 | 0 | SupportNavItem, supportNav, gatedSections, TicketStatus, TicketPriority, ticketCategories, FieldRow, statusDot, behaviourLabel, defaultStatuses, defaultCategories, defaultPriorities, defaultTypes, defaultChannels, exportReports, exportFormats, widgetPositions, openStatuses |
| [src/lib/theme.tsx](../../src/lib/theme.tsx) | 128 | 0 | 0 | ThemePreference, Theme, ThemeValue, storedPreference, ThemeProvider, useTheme |
| [src/lib/tones.ts](../../src/lib/tones.ts) | 21 | 0 | 0 | toneFor |
| [src/lib/travelExpenseData.ts](../../src/lib/travelExpenseData.ts) | 135 | 0 | 0 | TeTabId, currencies, SettingsItem, settingsNav, policyCategories, segmentModes, travelNotificationEvents, expenseCategories, RunStage, currencySymbol, money |
| [src/lib/types.ts](../../src/lib/types.ts) | 21 | 0 | 0 | User, OnboardingProfile, Session |
| [src/lib/validation.ts](../../src/lib/validation.ts) | 49 | 0 | 0 | passwordRules, passwordScore, validateFullName, validateEmail, validateOrganization, validatePassword, validateConfirm |
| [src/lib/vibeData.ts](../../src/lib/vibeData.ts) | 64 | 0 | 0 | Capability, vibeTopNav, vibeControls, vibeModels, capabilities |
| [src/lib/workspace.tsx](../../src/lib/workspace.tsx) | 2426 | 0 | 0 | Artifact, Notification, Run, Persisted, ApprovalDecision, AuditEvent, auditEvent, Connection, CustomConnector, Runner, PosCustomer, PosDoc, PosShift, MatchSettings, PosTaxCategory, PosCustomerGroup, PosLoyaltyProgramme, PosCashVariance, PosCustomField, PosChange, AppRecord, PosSettings, Rfp, KbDoc, PitchTemplate, BrandKit, CustomSchema, Asset, AssetRequest, ApprovalLevel, AssetSettings, SupportRule, SupportSettings, Ticket, KbArticle, CannedResponse, TeSettings, ExpenseReport, TravelRequest, CardTransaction, ReimbursementRun, Dpia, AutomatedDecision, RetentionPolicy, PortalPermission, PortalRole, GuardrailPolicy, Company, IntercompanyPair, ScheduleKind, Schedule, ChecklistTask, WorkspaceValue, defaultInstalled, empty, renamedCodes, reconcileInstalled, migrate, read, WorkspaceProvider, artifact, run, entity, entry, runner, entry, entry, entry, entry, order, entry, run, stages, entry, entry, entry, entry, prefixes, entry, entry, entry, useWorkspace |
| [src/routes/Protected.tsx](../../src/routes/Protected.tsx) | 24 | 0 | 2 | Protected |
| [src/screens/Landing.tsx](../../src/screens/Landing.tsx) | 829 | 12 | 10 | greeting, Section, Hero, SocialProof, Outcomes, outcomes, IndustryMarquee, track, Suites, VibeSection, AgentSection, AppsSection, PipelineSection, IntegrationsSection, MarketplaceSection, TrustSection, WhyUsSection, NewsSection, PricingSection, FaqSection, FinalCta, Landing |
| [src/screens/Login.tsx](../../src/screens/Login.tsx) | 122 | 3 | 2 | Login, onSubmit, next, onSso |
| [src/screens/Onboarding.tsx](../../src/screens/Onboarding.tsx) | 210 | 5 | 1 | Onboarding, next |
| [src/screens/ProductPage.tsx](../../src/screens/ProductPage.tsx) | 177 | 1 | 2 | colClass, CtaRow, SectionBlock, ProductPage |
| [src/screens/PublicMarketplace.tsx](../../src/screens/PublicMarketplace.tsx) | 136 | 4 | 2 | PublicMarketplace, all |
| [src/screens/Register.tsx](../../src/screens/Register.tsx) | 249 | 5 | 3 | Errors, Register, onSubmit, next, onSso |
| [src/screens/app/AdminScheduledJobs.tsx](../../src/screens/app/AdminScheduledJobs.tsx) | 216 | 9 | 0 | Cadence, cron, describe, nextRun, NewAutomation, AdminScheduledJobs |
| [src/screens/app/AgentPortal.tsx](../../src/screens/app/AgentPortal.tsx) | 289 | 3 | 2 | FilterId, AgentCard, RecentRuns, EmptyFilter, AgentPortal |
| [src/screens/app/AgentStudio.tsx](../../src/screens/app/AgentStudio.tsx) | 161 | 4 | 0 | canvasNodes, AgentStudio, testRun |
| [src/screens/app/Approvals.tsx](../../src/screens/app/Approvals.tsx) | 267 | 5 | 0 | decisionTone, QueueRow, Detail, Approvals |
| [src/screens/app/ClientPortal.tsx](../../src/screens/app/ClientPortal.tsx) | 403 | 13 | 2 | Tab, useEnabledTabs, TabsPane, NewRoleDialog, toggle, toggleHidden, RolesPane, UsersPane, AuditPane, ClientPortal |
| [src/screens/app/ComplianceCenter.tsx](../../src/screens/app/ComplianceCenter.tsx) | 900 | 40 | 0 | Metric, EmptyTable, Dashboard, Field, DpiaPane, DecisionsPane, InventoryPane, openFor, FlowsPane, RetentionPane, ComplianceCenter |
| [src/screens/app/GenericPortal.tsx](../../src/screens/app/GenericPortal.tsx) | 102 | 1 | 1 | GenericPortal |
| [src/screens/app/Governance.tsx](../../src/screens/app/Governance.tsx) | 380 | 8 | 0 | SortKey, RangeLabel, rowOptions, formatStamp, Dropdown, SortHeader, download, Governance, toggleSort, exportRows |
| [src/screens/app/Guardrails.tsx](../../src/screens/app/Guardrails.tsx) | 334 | 14 | 0 | Draft, blank, PolicyForm, next, PoliciesTab, scopes, ViolationsTab, Guardrails |
| [src/screens/app/Integrations.tsx](../../src/screens/app/Integrations.tsx) | 453 | 23 | 1 | authTone, AuthChip, ConnectionDialog, OAuthAppsDialog, CustomConnectorDialog, importSpec, Integrations |
| [src/screens/app/Marketplace.tsx](../../src/screens/app/Marketplace.tsx) | 367 | 8 | 1 | TabId, View, Sort, sorts, kindTone, AppIcon, InstallControls, AppCard, EmptyCatalog, Marketplace, tabs |
| [src/screens/app/Overview.tsx](../../src/screens/app/Overview.tsx) | 293 | 8 | 1 | greeting, TwoFactorBanner, TrialCard, Checklist, DashboardTabs, Overview |
| [src/screens/app/PortalPanel.tsx](../../src/screens/app/PortalPanel.tsx) | 68 | 0 | 0 | Notice, PanelHeader, DataTable, EmptyPanel |
| [src/screens/app/PromptLab.tsx](../../src/screens/app/PromptLab.tsx) | 246 | 8 | 0 | models, templates, Result, PromptLab, variables, run |
| [src/screens/app/Runners.tsx](../../src/screens/app/Runners.tsx) | 186 | 10 | 0 | statusLabel, PairDialog, copy, Runners |
| [src/screens/app/Runs.tsx](../../src/screens/app/Runs.tsx) | 101 | 1 | 0 | kindStyles, Runs |
| [src/screens/app/ScheduledJobs.tsx](../../src/screens/app/ScheduledJobs.tsx) | 147 | 5 | 0 | typeOptions, FilterSelect, SchedulesCard, ScheduledJobs |
| [src/screens/app/VibeStudio.tsx](../../src/screens/app/VibeStudio.tsx) | 259 | 8 | 0 | buildSteps, toArtifactName, TopBar, CapabilityCard, VibeStudio, build |
| [src/screens/app/assets/AssetManagement.tsx](../../src/screens/app/assets/AssetManagement.tsx) | 98 | 1 | 1 | AssetManagement |
| [src/screens/app/assets/panes.tsx](../../src/screens/app/assets/panes.tsx) | 816 | 33 | 0 | stageTone, AssetDashboard, tiles, AssetRegisterPane, projects, exportCsv, importCsv, MyAssetPane, AssetRequestsPane, AssetReportsPane, exportCsv |
| [src/screens/app/assets/settings.tsx](../../src/screens/app/assets/settings.tsx) | 699 | 29 | 0 | CopilotBanner, ApprovalLevelsPane, CustomFieldsPane, AgentsPane, SchedulesPane, ChangeHistoryPane, AssetSettingsPane, panes, MasterTaxonomiesPane |
| [src/screens/app/business/Chat.tsx](../../src/screens/app/business/Chat.tsx) | 205 | 7 | 1 | Message, BusinessChat, send, onKeyDown |
| [src/screens/app/business/Suite.tsx](../../src/screens/app/business/Suite.tsx) | 129 | 0 | 5 | CategoryView, BusinessSuite |
| [src/screens/app/business/Tool.tsx](../../src/screens/app/business/Tool.tsx) | 80 | 2 | 2 | BusinessTool, run |
| [src/screens/app/business/shared.tsx](../../src/screens/app/business/shared.tsx) | 46 | 0 | 2 | SuiteBanner, CreditChip, ToolCard, BackLink |
| [src/screens/app/crm/Crm.tsx](../../src/screens/app/crm/Crm.tsx) | 51 | 0 | 0 | panels, Crm |
| [src/screens/app/crm/panels.tsx](../../src/screens/app/crm/panels.tsx) | 970 | 61 | 0 | greeting, CrmDashboard, kpis, CrmLeads, CrmContacts, CrmCompanies, CrmDeals, CrmCalendar, CrmActivities, CrmSequences, CrmReports, kpis, statuses, CrmSettings |
| [src/screens/app/crm/records.tsx](../../src/screens/app/crm/records.tsx) | 187 | 1 | 0 | crmModals, useRecords |
| [src/screens/app/dashboardPanels.tsx](../../src/screens/app/dashboardPanels.tsx) | 404 | 3 | 12 | Stat, MeterCard, RunsRange, RunsRangeFilter, OverviewPanel, outcomes, AppMetricsPanel |
| [src/screens/app/hr/AttendanceCalendar.tsx](../../src/screens/app/hr/AttendanceCalendar.tsx) | 168 | 5 | 0 | AttendanceCalendar |
| [src/screens/app/hr/Hr.tsx](../../src/screens/app/hr/Hr.tsx) | 31 | 0 | 0 | Hr |
| [src/screens/app/hr/panels.tsx](../../src/screens/app/hr/panels.tsx) | 612 | 28 | 1 | HrDashboard, kpis, queues, SettingList, HrSettings, StatCard, UnderlineTabs, HrPagePanel, ViewToggle |
| [src/screens/app/pitch/BrandKit.tsx](../../src/screens/app/pitch/BrandKit.tsx) | 225 | 11 | 1 | swatches, BrandKitPane |
| [src/screens/app/pitch/PitchPilot.tsx](../../src/screens/app/pitch/PitchPilot.tsx) | 128 | 0 | 3 | panes, PitchPilot |
| [src/screens/app/pitch/panes.tsx](../../src/screens/app/pitch/panes.tsx) | 719 | 28 | 6 | Crumb, PitchDashboard, tiles, RfpInbox, exportCsv, NewRfp, schemas, extract, KnowledgeBase, Templates, upload, ExtractionSchema, all, Analytics |
| [src/screens/app/portals.tsx](../../src/screens/app/portals.tsx) | 83 | 0 | 0 |  |
| [src/screens/app/pos/SalesPos.tsx](../../src/screens/app/pos/SalesPos.tsx) | 261 | 5 | 1 | SalesPos, panes |
| [src/screens/app/pos/panes.tsx](../../src/screens/app/pos/panes.tsx) | 1186 | 31 | 0 | useLedger, PosDashboard, stageCounts, CustomersPane, exportCsv, importCsv, QuotationsPane, PosTillPane, ShiftsPane, cashierBands, ArAgingPane, PosReportsPane, MatchSettingsPane |
| [src/screens/app/pos/parts.tsx](../../src/screens/app/pos/parts.tsx) | 236 | 10 | 0 | PageHead, Stat, Bar, Empty, Select, DocListPane |
| [src/screens/app/pos/settings.tsx](../../src/screens/app/pos/settings.tsx) | 911 | 39 | 0 | PaneHead, Card, EmptyBox, TaxPane, GroupsPane, LoyaltyPane, varianceDefaults, VariancePane, FieldsPane, AgentsPane, SchedulesPane, HistoryPane, AppearancePane, panes, PosSettingsPane |
| [src/screens/app/setup/Companies.tsx](../../src/screens/app/setup/Companies.tsx) | 463 | 23 | 0 | currencies, countries, Chip, tones, CompanyForm, save, patch, CompaniesTab, PairsTab, ConsolidatedTab, Companies |
| [src/screens/app/setup/Setup.tsx](../../src/screens/app/setup/Setup.tsx) | 54 | 0 | 1 | cards, Setup |
| [src/screens/app/setup/useCompanies.ts](../../src/screens/app/setup/useCompanies.ts) | 33 | 0 | 0 | useCompanies, primary |
| [src/screens/app/support/SupportTicketing.tsx](../../src/screens/app/support/SupportTicketing.tsx) | 97 | 0 | 1 | panes, SupportTicketing |
| [src/screens/app/support/panes.tsx](../../src/screens/app/support/panes.tsx) | 962 | 50 | 1 | priorityTone, PlanGate, Kpi, Breakdown, SupportDashboard, TicketsPane, statuses, sources, KnowledgeBasePane, onImport, MyRequestsPane, CannedRepliesPane, SupportReportsPane |
| [src/screens/app/support/settings.tsx](../../src/screens/app/support/settings.tsx) | 1190 | 48 | 0 | useSettings, Hint, SectionHead, fieldCopy, TicketFieldsPane, persist, WebWidgetPane, CsatSurveyPane, RuleListPane, ParseFailuresPane, CsatReviewPane, ReportExportsPane, download, TiersSearchPane, SupportAgentsPane, EscalationActions, SupportSettingsPane, panes |
| [src/screens/app/travel/TravelExpense.tsx](../../src/screens/app/travel/TravelExpense.tsx) | 91 | 1 | 1 | TravelExpense |
| [src/screens/app/travel/panes.tsx](../../src/screens/app/travel/panes.tsx) | 820 | 31 | 0 | statusTone, nextAction, DashboardPane, pipeline, ExpenseReportsPane, create, CardsPane, onFile, ReimbursementsPane, TravelPane, AgencyReviewPane, ApprovalInboxPane, ReportsPane, summary |
| [src/screens/app/travel/settings.tsx](../../src/screens/app/travel/settings.tsx) | 676 | 31 | 2 | useDraft, ExpensePolicyPane, ReceiptEmailPane, TravelPolicyPane, SegmentFieldsPane, TravelNotificationsPane, ApprovalLevelsPane, SlaPane, GlAccountsPane, CustomFieldsPane, AgentsPane, SchedulesPane, ChangeHistoryPane, AppearancePane, panes, SettingsPane |
| [src/screens/app/workspace.tsx](../../src/screens/app/workspace.tsx) | 203 | 3 | 3 | Inbox, Account |
| [src/screens/studio/AgentStudio.tsx](../../src/screens/studio/AgentStudio.tsx) | 507 | 18 | 4 | Sidebar, AgentHealth, MiniCalendar, AgentStudio |

## Reviewable interaction index

### src/components/AppChrome.tsx

- **src/components/AppChrome.tsx:60** **button** <span aria-hidden className="text-[13px]"> {tab.icon} </span> {tab.label} — onClick={() => setParams(tab.id === tabs[0].id ? {} : { tab: tab.id }, { replace: true })} — UNVERIFIED
- **src/components/AppChrome.tsx:98** **button** {children} — onClick={onClick} — UNVERIFIED
- **src/components/AppChrome.tsx:121** **select** {[label, ...options.filter((option) => option !== label)].map((option) => ( <option key={option}>{option}</option> ))} — onChange={(event) => setValue(event.target.value)} — UNVERIFIED
- **src/components/AppChrome.tsx:140** **input**  — placeholder={placeholder}; onChange={(event) => onChange?.(event.target.value)} — UNVERIFIED
- **src/components/AppChrome.tsx:163** **button** {option} — onClick={() => onChange(option)} — UNVERIFIED
- **src/components/AppChrome.tsx:188** **button** {children} — onClick={onClick} — UNVERIFIED

### src/components/AppShell.tsx

- **src/components/AppShell.tsx:17** **button** ☰ Menu — type="button"; onClick={onOpenNavigation} — UNVERIFIED

### src/components/AppSideChrome.tsx

- **src/components/AppSideChrome.tsx:72** **button** <span aria-hidden className="text-[10px]"> {isOpen ? '⌄' : '›'} </span> {group.label} — onClick={() => setCollapsed((prev) => prev.includes(group.id) ? prev.filter((id) => id !== group.id) : [...prev, group.id], ) } — UNVERIFIED
- **src/components/AppSideChrome.tsx:93** **button** <span aria-hidden className="w-4 shrink-0 text-center text-[13px]"> {item.icon} </span> {item.label} — onClick={() => setParams(item.id === defaultPage ? {} : { tab: item.id }, { replace: true })} — UNVERIFIED
- **src/components/AppSideChrome.tsx:134** **button** {tab} — onClick={() => onChange(tab)} — UNVERIFIED

### src/components/AppSidebar.tsx

- **src/components/AppSidebar.tsx:133** **button** {group.label} {group.collapsible && ( <span aria-hidden className="text-[10px]"> {open ? '⌄' : '›'} </span> )} — onClick={() => group.collapsible && setOpen((prev) => !prev)} — UNVERIFIED
- **src/components/AppSidebar.tsx:204** **button** {collapsed ? '›' : '‹'} — onClick={onToggleCollapsed} — UNVERIFIED

### src/components/CopilotDock.tsx

- **src/components/CopilotDock.tsx:54** **button** ✕ — onClick={() => setOpen(false)} — UNVERIFIED
- **src/components/CopilotDock.tsx:72** **form** <input value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Ask or instruct…" aria-label="Ask the AI Copilot" className="min-w-0  — onSubmit={(event) => { event.preventDefault() ask() }} — UNVERIFIED
- **src/components/CopilotDock.tsx:79** **input**  — onChange={(event) => setQuestion(event.target.value)}; placeholder="Ask or instruct…" — UNVERIFIED
- **src/components/CopilotDock.tsx:86** **button** ➤ — type="submit" — UNVERIFIED
- **src/components/CopilotDock.tsx:93** **button** {open ? '✕' : '✨'} — onClick={() => setOpen((prev) => !prev)} — UNVERIFIED

### src/components/Dialog.tsx

- **src/components/Dialog.tsx:39** **button** ✕ — type="button"; onClick={onClose} — UNVERIFIED

### src/components/NavGroup.tsx

- **src/components/NavGroup.tsx:21** **button** <span aria-hidden className="text-[9px]"> {open ? '⌄' : '›'} </span> {group} — onClick={() => setOpen((prev) => !prev)} — UNVERIFIED
- **src/components/NavGroup.tsx:35** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => onSelect(item.id)} — UNVERIFIED

### src/components/RecordDialog.tsx

- **src/components/RecordDialog.tsx:54** **input**  — type="checkbox"; onChange={(event) => onChange(event.target.checked ? 'Yes' : '')} — UNVERIFIED
- **src/components/RecordDialog.tsx:72** **button** {field.action} — type="button"; disabled=true — UNVERIFIED
- **src/components/RecordDialog.tsx:90** **textarea**  — onChange={(event) => onChange(event.target.value)}; placeholder={field.placeholder} — UNVERIFIED
- **src/components/RecordDialog.tsx:105** **select** {(field.options ?? [field.value]).map((option) => <option key={option}>{option}</option>)} — onChange={(event) => onChange(event.target.value)} — UNVERIFIED
- **src/components/RecordDialog.tsx:117** **input**  — type="date"; onChange={(event) => onChange(event.target.value)} — UNVERIFIED
- **src/components/RecordDialog.tsx:131** **input**  — type={isNumber ? 'number' : 'text'}; onChange={(event) => onChange(event.target.value)}; placeholder={isNumber ? undefined : field.placeholder} — UNVERIFIED
- **src/components/RecordDialog.tsx:199** **button** Cancel — onClick={onClose} — UNVERIFIED
- **src/components/RecordDialog.tsx:202** **button** {modal.submit} — onClick={submit}; disabled={missing} — UNVERIFIED

### src/components/RecordList.tsx

- **src/components/RecordList.tsx:14** **button** Delete — onClick={() => onDelete(record.id)} — UNVERIFIED

### src/components/SiteRail.tsx

- **src/components/SiteRail.tsx:87** **button** <span aria-hidden className="text-base leading-none"> ⋮ </span> <span className="mt-1">More</span> — onClick={() => setMoreOpen((prev) => !prev)} — UNVERIFIED
- **src/components/SiteRail.tsx:104** **button** <Icon name={option.icon} size={14} /> <span className="mt-0.5">{option.label}</span> — onClick={() => setPreference(option.id)} — UNVERIFIED

### src/components/ThemeSwitch.tsx

- **src/components/ThemeSwitch.tsx:33** **button** <Icon name={option.icon} size={compact ? 14 : 15} /> {!compact && option.label} — onClick={() => setPreference(option.id)} — UNVERIFIED

### src/components/VippyWidget.tsx

- **src/components/VippyWidget.tsx:33** **button** ✕ — onClick={() => setOpen(false)} — UNVERIFIED
- **src/components/VippyWidget.tsx:51** **form** <input value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Ask Vippy anything" aria-label="Ask Vippy" className="min-w-0 flex-1  — onSubmit={(event) => { event.preventDefault() send() }} — UNVERIFIED
- **src/components/VippyWidget.tsx:58** **input**  — onChange={(event) => setQuestion(event.target.value)}; placeholder="Ask Vippy anything" — UNVERIFIED
- **src/components/VippyWidget.tsx:65** **button** ➤ — type="submit" — UNVERIFIED
- **src/components/VippyWidget.tsx:74** **button** ✦ Ask <span className="font-semibold text-accent">Vippy</span> anything — onClick={() => setOpen(true)} — UNVERIFIED
- **src/components/VippyWidget.tsx:81** **button** {open ? '✕' : '💬'} — onClick={() => setOpen((prev) => !prev)} — UNVERIFIED

### src/components/ui.tsx

- **src/components/ui.tsx:35** **button** {loading && ( <span aria-hidden className="h-3.5 w-3.5 animate-spin rounded-full border-2 border-current border-t-transparent" /> )} {children} — disabled={rest.disabled || loading} — UNVERIFIED
- **src/components/ui.tsx:69** **input**  —  — UNVERIFIED
- **src/components/ui.tsx:112** **Button** <GoogleMark /> Continue with Google — disabled={disabled}; onClick={() => onPick('google')}; type="button" — UNVERIFIED
- **src/components/ui.tsx:115** **Button** <MicrosoftMark /> Continue with Microsoft — disabled={disabled}; onClick={() => onPick('microsoft')}; type="button" — UNVERIFIED

### src/screens/Landing.tsx

- **src/screens/Landing.tsx:103** **form** <span aria-hidden className="pl-2 text-accent"> ✦ </span> <input value={prompt} onChange={(event) => setPrompt(event.target.value)} aria-label="Ask Apragya anyt — onSubmit={(event) => { event.preventDefault() start(prompt) }} — UNVERIFIED
- **src/screens/Landing.tsx:113** **input**  — onChange={(event) => setPrompt(event.target.value)}; placeholder="Ask Apragya anything — build an agent, automate a workflow…" — UNVERIFIED
- **src/screens/Landing.tsx:120** **Button** Start free — type="submit" — UNVERIFIED
- **src/screens/Landing.tsx:129** **button** <span aria-hidden className="mr-1.5"> {chip.icon} </span> {chip.label} — type="button"; onClick={() => start(chip.label)} — UNVERIFIED
- **src/screens/Landing.tsx:348** **button** <span className="text-[11px] font-bold text-accent">{item.n}</span> <h3 className="mt-2 text-sm font-semibold">{item.name}</h3> <p className="mt-1 text-xs text- — onClick={() => setActive(index)} — UNVERIFIED
- **src/screens/Landing.tsx:425** **button** {item.name} — onClick={() => setActive(index)} — UNVERIFIED
- **src/screens/Landing.tsx:630** **button** {label} — onClick={() => setAnnual(isAnnual)} — UNVERIFIED
- **src/screens/Landing.tsx:700** **Button** {plan.cta.label} —  — UNVERIFIED
- **src/screens/Landing.tsx:722** **button** {item.group} — onClick={() => setGroup(index)} — UNVERIFIED
- **src/screens/Landing.tsx:742** **button** <span className="text-[14px] font-medium">{item.q}</span> <span aria-hidden className="text-fg-muted"> {isOpen ? '−' : '+'} </span> — onClick={() => setOpen(isOpen ? null : item.q)} — UNVERIFIED
- **src/screens/Landing.tsx:780** **Button** Create your workspace → —  — UNVERIFIED
- **src/screens/Landing.tsx:785** **Button** Talk to sales —  — UNVERIFIED

### src/screens/Login.tsx

- **src/screens/Login.tsx:72** **SsoButtons**  — disabled={submitting} — UNVERIFIED
- **src/screens/Login.tsx:77** **form** <Field label="Work email" type="email" placeholder="you@company.com" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} error — onSubmit={onSubmit} — UNVERIFIED
- **src/screens/Login.tsx:111** **Button** Sign in — type="submit" — UNVERIFIED

### src/screens/Onboarding.tsx

- **src/screens/Onboarding.tsx:110** **button** {size} — type="button"; onClick={() => setTeamSize(size)} — UNVERIFIED
- **src/screens/Onboarding.tsx:138** **button** {option} — type="button"; onClick={() => setIndustry(option)} — UNVERIFIED
- **src/screens/Onboarding.tsx:168** **button** <span aria-hidden className="text-base">{app.icon}</span> <p className="mt-1.5 text-sm font-semibold">{app.name}</p> <p className="text-xs text-fg-muted">{app.b — type="button"; onClick={() => toggleApp(app.code)} — UNVERIFIED
- **src/screens/Onboarding.tsx:194** **Button** ← Back — type="button"; onClick={() => (step === 0 ? navigate('/') : setStep(step - 1))} — UNVERIFIED
- **src/screens/Onboarding.tsx:201** **Button** {step === steps.length - 1 ? 'Open my workspace' : 'Continue'} — type="button"; onClick={next} — UNVERIFIED

### src/screens/ProductPage.tsx

- **src/screens/ProductPage.tsx:19** **Button** {cta.label} —  — UNVERIFIED

### src/screens/PublicMarketplace.tsx

- **src/screens/PublicMarketplace.tsx:54** **Button** Start Free —  — UNVERIFIED
- **src/screens/PublicMarketplace.tsx:82** **button** {item} — onClick={() => setFilter(item)} — UNVERIFIED
- **src/screens/PublicMarketplace.tsx:95** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search the catalog…" — UNVERIFIED
- **src/screens/PublicMarketplace.tsx:119** **Button** Install —  — UNVERIFIED

### src/screens/Register.tsx

- **src/screens/Register.tsx:116** **SsoButtons**  — disabled={submitting} — UNVERIFIED
- **src/screens/Register.tsx:121** **form** {/* Honeypot — hidden from people, visible to naive bots. */} <div aria-hidden className="absolute -left-[9999px]"> <label htmlFor="website">Website (leave empt — onSubmit={onSubmit} — UNVERIFIED
- **src/screens/Register.tsx:125** **input**  — onChange={set('website')} — UNVERIFIED
- **src/screens/Register.tsx:214** **input**  — type="checkbox"; onChange={(event) => setAgreed(event.target.checked)} — UNVERIFIED
- **src/screens/Register.tsx:240** **Button** Create Account — type="submit" — UNVERIFIED

### src/screens/app/AdminScheduledJobs.tsx

- **src/screens/app/AdminScheduledJobs.tsx:71** **button** ✕ — onClick={onClose} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:79** **select** <option value="">Select an app...</option> {apps.map((item) => ( <option key={item.code} value={item.code}> {item.name} </option> ))} — onChange={(event) => { setAppCode(event.target.value) setAgent('') }} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:98** **select** <option value="">{app ? 'Select an agent...' : 'Pick an app first'}</option> {app?.agents.map((item) => ( <option key={item} value={item}> {item} </option> ))} — disabled={!app}; onChange={(event) => setAgent(event.target.value)} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:115** **input**  — onChange={(event) => setName(event.target.value)}; placeholder="e.g. Nightly allocation run" — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:126** **select** {cadences.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setCadence(event.target.value as Cadence)} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:136** **select** {hours.map((item) => ( <option key={item}>{item}</option> ))} — disabled={cadence === 'Hourly'}; onChange={(event) => setTime(event.target.value)} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:155** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:158** **Button** Create automation — disabled={!app || !agent || !name.trim()}; onClick={() => { addSchedule({ name: name.trim(), kind: 'Agent', target: `${app?.name} · ${agent}`, cadence: cadence === 'Hourly' ? 'Hourly' : `${cadence} · ${time}`, nextRun: nextRun(cadence, time), active:  — UNVERIFIED
- **src/screens/app/AdminScheduledJobs.tsx:203** **Button** + New Automation — onClick={() => setOpen(true)} — UNVERIFIED

### src/screens/app/AgentPortal.tsx

- **src/screens/app/AgentPortal.tsx:65** **button** Run — onClick={onRun} — UNVERIFIED
- **src/screens/app/AgentPortal.tsx:200** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search business cases, agents, workflows, automations..." — UNVERIFIED
- **src/screens/app/AgentPortal.tsx:211** **button** {item.icon && <span aria-hidden>{item.icon}</span>} {item.label} — onClick={() => setFilter(item.id)} — UNVERIFIED

### src/screens/app/AgentStudio.tsx

- **src/screens/app/AgentStudio.tsx:50** **Button** Run test — onClick={testRun} — UNVERIFIED
- **src/screens/app/AgentStudio.tsx:58** **button** {item} — onClick={() => setTab(item)} — UNVERIFIED
- **src/screens/app/AgentStudio.tsx:77** **button** <p className="text-[13px] font-semibold">{item.label}</p> <p className={`text-xs ${item.tone.includes('text-bg') ? 'text-white/60' : 'text-fg-muted'}`}> {item.k — onClick={() => setSelected(item.id)} — UNVERIFIED
- **src/screens/app/AgentStudio.tsx:152** **button** Runs — onClick={() => navigate('/app/runs')} — UNVERIFIED

### src/screens/app/Approvals.tsx

- **src/screens/app/Approvals.tsx:29** **button** <div className="flex items-start justify-between gap-2"> <p className="text-[14px] font-medium">{approval.agent}</p> {decision && ( <span className={`shrink-0 r — onClick={onSelect} — UNVERIFIED
- **src/screens/app/Approvals.tsx:112** **button** Approve — onClick={() => onDecide('approved')} — UNVERIFIED
- **src/screens/app/Approvals.tsx:118** **button** Modify — onClick={() => onDecide('modified')} — UNVERIFIED
- **src/screens/app/Approvals.tsx:124** **button** Reject — onClick={() => onDecide('rejected')} — UNVERIFIED
- **src/screens/app/Approvals.tsx:169** **button** ⟳ — onClick={reload} — UNVERIFIED

### src/screens/app/ClientPortal.tsx

- **src/screens/app/ClientPortal.tsx:43** **input**  — type="checkbox"; onChange={(event) => setDraft((prev) => event.target.checked ? [...prev, tab.id] : prev.filter((id) => id !== tab.id), ) } — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:66** **Button** Save changes — disabled={!dirty}; onClick={() => setPortalTabs(draft)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:115** **input**  — onChange={(event) => setName(event.target.value)}; placeholder="e.g. portal_viewer" — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:124** **input**  — onChange={(event) => setDescription(event.target.value)}; placeholder="Short label admins see" — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:134** **input**  — type="checkbox"; onChange={(event) => setIsDefault(event.target.checked)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:145** **select** {portalScopes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setScope(event.target.value)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:184** **input**  — type="checkbox"; onChange={() => toggle(tab.id, key)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:199** **button** {field} — onClick={() => toggleHidden(tab.id, field)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:223** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:226** **Button** ⎘ Create role — disabled={!name.trim()}; onClick={() => { addPortalRole({ name: name.trim(), description: description.trim(), isDefault, scope, permissions }) onClose() }} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:256** **Button** + New role — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:283** **Button** Delete — onClick={() => removePortalRole(role.id)} — UNVERIFIED
- **src/screens/app/ClientPortal.tsx:381** **button** {tab} — onClick={() => setParams({ tab: tab.toLowerCase() })} — UNVERIFIED

### src/screens/app/ComplianceCenter.tsx

- **src/screens/app/ComplianceCenter.tsx:150** **Button** + New DPIA — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:198** **input**  — onChange={(event) => setTitle(event.target.value)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:201** **input**  — onChange={(event) => setActivity(event.target.value)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:204** **select** {['Low', 'Medium', 'High'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setRisk(event.target.value as typeof risk)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:216** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:219** **Button** Create DPIA — disabled={!title.trim()}; onClick={() => { addDpia({ title: title.trim(), activity: activity.trim(), risk, status: 'Draft' }) setTitle('') setActivity('') setOpen(false) }} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:251** **Button** + New entry — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:302** **input**  — onChange={(event) => setName(event.target.value)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:305** **input**  — type="checkbox"; onChange={(event) => setProfiling(event.target.checked)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:314** **input**  — type="checkbox"; onChange={(event) => setHumanReview(event.target.checked)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:324** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:327** **Button** Register entry — disabled={!name.trim()}; onClick={() => { addAutomatedDecision({ name: name.trim(), profiling, humanReview, // Profiling with nobody in the loop is exactly what Art. 22 flags. status: profiling && !humanReview ? 'Needs review' : 'Reg — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:390** **button** <Icon name="search" size={15} /> Discover PII — onClick={() => setDiscovered( `Scan complete — no personal-data fields found beyond the ${dataInventory.length} already catalogued.`, ) } — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:400** **Button** + Add field — onClick={() => openFor(null)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:444** **button** Edit — onClick={() => openFor(item.id)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:450** **button** Delete — onClick={() => removeInventoryField(item.id)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:468** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, entity: event.target.value }))}; placeholder="users" — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:476** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, field: event.target.value }))}; placeholder="email" — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:484** **select** {inventoryCategories.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, category: event.target.value as InventoryCategory })) } — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:497** **select** {legalBases.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, legalBasis: event.target.value }))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:508** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, retention: event.target.value }))}; placeholder="Life of account" — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:516** **input**  — type="checkbox"; onChange={(event) => setDraft((prev) => ({ ...prev, sensitive: event.target.checked }))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:526** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:529** **Button** {editing ? 'Save field' : 'Add field'} — disabled={!draft.entity.trim() || !draft.field.trim()}; onClick={() => { if (editing) updateInventoryField(editing, draft) else addInventoryField(draft) setOpen(false) }} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:568** **button** {item === 'Map' ? '⇄ Map' : item} — onClick={() => setView(item)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:580** **Button** + New flow — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:674** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, name: event.target.value }))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:681** **select** {['ingress', 'egress', 'internal'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, type: event.target.value as typeof prev.type })) } — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:694** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, source: event.target.value }))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:701** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, destination: event.target.value }))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:708** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, crossBorder: event.target.value }))}; placeholder="Leave blank if the data stays in-region" — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:717** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:720** **Button** Map flow — disabled={!draft.name.trim()}; onClick={() => { addDataFlow(draft) setDraft({ name: '', type: 'egress', source: '', destination: '', crossBorder: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:750** **Button** + Set policy — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:787** **button** Delete — onClick={() => removeRetentionPolicy(policy.id)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:817** **select** {connectors.map((item) => ( <option key={item.id} value={item.id}> {item.name} </option> ))} — onChange={(event) => setConnectorId(event.target.value)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:830** **input**  — type="number"; onChange={(event) => setDays(Number(event.target.value))} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:840** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:843** **Button** Set policy — disabled={days < 1}; onClick={() => { addRetentionPolicy({ connectorId, keepForDays: days, enabled: true, lastRun: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/ComplianceCenter.tsx:878** **button** {tab.label} — onClick={() => setParams({ tab: tab.id })} — UNVERIFIED

### src/screens/app/GenericPortal.tsx

- **src/screens/app/GenericPortal.tsx:81** **button** Run — onClick={() => recordRun({ agent: agent.name, source: app.name })} — UNVERIFIED

### src/screens/app/Governance.tsx

- **src/screens/app/Governance.tsx:51** **select** {options.map((option) => ( <option key={option} value={option}> {option} </option> ))} — onChange={(event) => onChange(event.target.value)} — UNVERIFIED
- **src/screens/app/Governance.tsx:85** **button** {label} <span aria-hidden className={active ? 'text-accent' : 'text-fg-muted'}> {active ? (sort.desc ? '↓' : '↑') : '⇅'} </span> — onClick={() => onSort(column)} — UNVERIFIED
- **src/screens/app/Governance.tsx:231** **button** <Icon name="download" size={15} /> Export CSV — onClick={() => exportRows('csv')} — UNVERIFIED
- **src/screens/app/Governance.tsx:237** **button** <Icon name="download" size={15} /> Export JSON — onClick={() => exportRows('json')} — UNVERIFIED
- **src/screens/app/Governance.tsx:254** **input**  — onChange={(event) => { setQuery(event.target.value) setPage(1) }}; placeholder="Search logs..." — UNVERIFIED
- **src/screens/app/Governance.tsx:339** **select** {rowOptions.map((option) => ( <option key={option} value={option}> {option} </option> ))} — onChange={(event) => { setPerPage(Number(event.target.value)) setPage(1) }} — UNVERIFIED
- **src/screens/app/Governance.tsx:354** **button** ‹ — onClick={() => setPage((value) => Math.max(1, value - 1))}; disabled={current === 1} — UNVERIFIED
- **src/screens/app/Governance.tsx:367** **button** › — onClick={() => setPage((value) => Math.min(pageCount, value + 1))}; disabled={current === pageCount} — UNVERIFIED

### src/screens/app/Guardrails.tsx

- **src/screens/app/Guardrails.tsx:59** **input**  — onChange={(event) => set('name', event.target.value)}; placeholder="e.g. Approval cap — ₹50,000" — UNVERIFIED
- **src/screens/app/Guardrails.tsx:68** **select** {guardrailTypes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => set('type', event.target.value as GuardrailType)} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:80** **select** {guardrailActions.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => set('action', event.target.value as GuardrailAction)} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:92** **select** {checkPoints.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => set('checkPoint', event.target.value as CheckPoint)} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:104** **select** {scopes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => set('scope', event.target.value)} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:117** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:120** **Button** {policy ? 'Save policy' : 'Create policy'} — disabled={!draft.name.trim()}; onClick={() => { const next = { ...draft, name: draft.name.trim() } if (policy) updatePolicy(policy.id, next) else addPolicy(next) onClose() }} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:153** **Button** + Create Policy — onClick={() => setForm({ open: true, policy: null })} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:190** **button** <span aria-hidden>⏻</span> {policy.active ? 'Active' : 'Paused'} — onClick={() => updatePolicy(policy.id, { active: !policy.active })} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:203** **button** ✎ — onClick={() => setForm({ open: true, policy })} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:210** **button** 🗑 — onClick={() => deletePolicy(policy.id)} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:250** **input**  — onChange={(event) => setRunId(event.target.value)}; placeholder="Enter Run ID to view violations..." — UNVERIFIED
- **src/screens/app/Guardrails.tsx:258** **Button** <Icon name="eye" size={15} /> View Violations — disabled={!runId.trim()}; onClick={() => setLooked(runId.trim())} — UNVERIFIED
- **src/screens/app/Guardrails.tsx:316** **button** <Icon name={item.id === 'policies' ? 'shield-check' : 'alert-triangle'} size={16} /> {item.label} — onClick={() => setParams({ tab: item.id })} — UNVERIFIED

### src/screens/app/Integrations.tsx

- **src/screens/app/Integrations.tsx:45** **select** {connectors.map((item) => ( <option key={item.id} value={item.id}> {item.name} </option> ))} — onChange={(event) => setConnectorId(event.target.value)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:59** **input**  — onChange={(event) => setName(event.target.value)}; placeholder="e.g. Finance Slack workspace" — UNVERIFIED
- **src/screens/app/Integrations.tsx:68** **input**  — onChange={(event) => setAccount(event.target.value)}; placeholder="host, workspace or account this connection points at" — UNVERIFIED
- **src/screens/app/Integrations.tsx:84** **input**  — type="password"; onChange={(event) => setSecret(event.target.value)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:102** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/Integrations.tsx:105** **Button** Save connection — disabled={!name.trim()}; onClick={() => { addConnection({ connectorId, name: name.trim(), account: account.trim(), secretHint: secret ? `••••${secret.slice(-4)}` : '', }) onClose() }} — UNVERIFIED
- **src/screens/app/Integrations.tsx:143** **Button** Close — onClick={onClose} — UNVERIFIED
- **src/screens/app/Integrations.tsx:195** **textarea**  — onChange={(event) => { setSpec(event.target.value) setError(null) }}; placeholder={'{\n "openapi": "3.0.0",\n "info": { "title": "Internal ERP" },\n "servers": [...]\n}'} — UNVERIFIED
- **src/screens/app/Integrations.tsx:207** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/Integrations.tsx:210** **Button** Import — disabled={!spec.trim()}; onClick={importSpec} — UNVERIFIED
- **src/screens/app/Integrations.tsx:220** **input**  — onChange={(event) => setName(event.target.value)}; placeholder="e.g. Internal ERP" — UNVERIFIED
- **src/screens/app/Integrations.tsx:229** **input**  — onChange={(event) => setBaseUrl(event.target.value)}; placeholder="https://erp.internal/api/v1" — UNVERIFIED
- **src/screens/app/Integrations.tsx:238** **select** {['api_key', 'basic', 'oauth2', 'none'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setAuth(event.target.value)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:250** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/Integrations.tsx:253** **Button** Create connector — disabled={!name.trim()}; onClick={() => { addCustomConnector({ name: name.trim(), baseUrl: baseUrl.trim(), auth, operations: 0, source: 'manual', }) onClose() }} — UNVERIFIED
- **src/screens/app/Integrations.tsx:303** **button** <Icon name="key" size={15} /> OAuth Apps — onClick={() => setOauthOpen(true)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:309** **Button** + Add connection — onClick={() => setAdding(true)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:336** **Button** Remove — onClick={() => removeConnection(connection.id)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:353** **Button** + Add connection — onClick={() => setAdding(true)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:369** **button** <span className="flex items-start justify-between gap-3"> <span className="text-[15px] font-semibold">{connector.name}</span> <AuthChip auth={connector.auth} /> — onClick={() => setConnectionFor(connector.id)} — UNVERIFIED
- **src/screens/app/Integrations.tsx:399** **button** <span aria-hidden>⎘</span> Import OpenAPI — onClick={() => setCustom('openapi')} — UNVERIFIED
- **src/screens/app/Integrations.tsx:405** **Button** + New connector — onClick={() => setCustom('manual')} — UNVERIFIED
- **src/screens/app/Integrations.tsx:422** **Button** Remove — onClick={() => removeCustomConnector(connector.id)} — UNVERIFIED

### src/screens/app/Marketplace.tsx

- **src/screens/app/Marketplace.tsx:44** **button** {disabledApps.includes(app.code) ? '▸ Enable' : '🔒 Disable'} — onClick={() => toggleAppDisabled(app.code)} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:55** **button** ✕ Uninstall — onClick={() => uninstall(app.code)} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:78** **button** {busy ? 'Installing…' : '⚡ Install'} — onClick={async () => { setBusy(true) // The live product quotes a 10–20s install; compressed here. await new Promise((resolve) => setTimeout(resolve, 800)) install(app.code) setBusy(false) }}; disabled={busy} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:143** **button** i — onClick={() => setAbout(true)} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:249** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search..." — UNVERIFIED
- **src/screens/app/Marketplace.tsx:258** **select** {sorts.map((option) => ( <option key={option}>{option}</option> ))} — onChange={(event) => setSort(event.target.value as Sort)} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:271** **button** {option === 'grid' ? '▦' : '☰'} — onClick={() => setView(option)} — UNVERIFIED
- **src/screens/app/Marketplace.tsx:290** **button** <span aria-hidden>{item.icon}</span> {item.label} <span className={tab === item.id ? 'text-white/70' : 'text-fg-muted'}>({item.count})</span> — onClick={() => setTab(item.id)} — UNVERIFIED

### src/screens/app/Overview.tsx

- **src/screens/app/Overview.tsx:33** **button** Turn on now — onClick={() => setTwoFactor('on')} — UNVERIFIED
- **src/screens/app/Overview.tsx:39** **button** ✕ Remind me later — onClick={() => setTwoFactor('snoozed')} — UNVERIFIED
- **src/screens/app/Overview.tsx:110** **button** ✕ — onClick={() => setFlag('dismissedChecklist')} — UNVERIFIED
- **src/screens/app/Overview.tsx:139** **button** <span aria-hidden className={`grid h-6 w-6 shrink-0 place-items-center rounded-full text-[12px] ${ task.done ? 'bg-ok text-white' : 'border-2 border-bad/40 text — onClick={() => { if (task.action === 'allocate') setFlag('creditsAllocated') else if (task.action === 'invite') setFlag('invitedTeammate') else if (task.to) navigate(task.to) }} — UNVERIFIED
- **src/screens/app/Overview.tsx:197** **button** <span aria-hidden>▦</span> Overview — onClick={() => setTab('overview')} — UNVERIFIED
- **src/screens/app/Overview.tsx:210** **button** <span aria-hidden className={`h-1.5 w-1.5 rounded-full ${code === 'CRM' ? 'bg-sky-500' : 'bg-violet-500'}`} /> {app.name} — onClick={() => setTab(code)} — UNVERIFIED
- **src/screens/app/Overview.tsx:228** **button** {item} — onClick={() => setPeriod(item)} — UNVERIFIED
- **src/screens/app/Overview.tsx:246** **button** {action.icon} — onClick={action.run} — UNVERIFIED

### src/screens/app/PromptLab.tsx

- **src/screens/app/PromptLab.tsx:79** **select** {models.map((option) => ( <option key={option}>{option}</option> ))} — onChange={(event) => setModel(event.target.value)} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:92** **input**  — type="range"; onChange={(event) => setTemperature(Number(event.target.value))} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:106** **input**  — type="number"; onChange={(event) => setMaxTokens(Number(event.target.value))} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:115** **button** <span aria-hidden>✦</span> Templates — onClick={() => setTemplatesOpen((prev) => !prev)} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:127** **button** <span className="block text-[14px] font-medium">{template.name}</span> <span className="mt-0.5 block truncate text-[12px] text-fg-muted">{template.user}</span> — onClick={() => { setSystem(template.system) setUser(template.user) setTemplatesOpen(false) }} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:144** **button** {running ? ( <span aria-hidden className="h-3.5 w-3.5 animate-spin rounded-full border-2 border-current border-t-transparent" /> ) : ( <span aria-hidden>▷</span — onClick={run}; disabled={!user.trim() || running} — UNVERIFIED
- **src/screens/app/PromptLab.tsx:163** **textarea**  — onChange={(event) => setSystem(event.target.value)}; placeholder="You are a helpful assistant..." — UNVERIFIED
- **src/screens/app/PromptLab.tsx:177** **textarea**  — onChange={(event) => setUser(event.target.value)}; placeholder="Enter your prompt here... Use {{variable}} for placeholders." — UNVERIFIED

### src/screens/app/Runners.tsx

- **src/screens/app/Runners.tsx:41** **button** ✕ — onClick={onClose} — UNVERIFIED
- **src/screens/app/Runners.tsx:58** **button** {copied === 'token' ? 'Copied' : 'Copy'} — onClick={() => copy('token', issued.token)} — UNVERIFIED
- **src/screens/app/Runners.tsx:73** **button** {copied === 'command' ? 'Copied' : 'Copy'} — onClick={() => copy('command', `apragya runner install --token ${issued.token}`)} — UNVERIFIED
- **src/screens/app/Runners.tsx:83** **Button** Done — onClick={onClose} — UNVERIFIED
- **src/screens/app/Runners.tsx:93** **input**  — onChange={(event) => setName(event.target.value)}; placeholder="corp-network-prod" — UNVERIFIED
- **src/screens/app/Runners.tsx:102** **input**  — onChange={(event) => setDescription(event.target.value)}; placeholder="Runs in our datacenter VPC, reaches SAP Gateway" — UNVERIFIED
- **src/screens/app/Runners.tsx:112** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/Runners.tsx:115** **Button** Generate pairing token — disabled={!name.trim()}; onClick={() => setIssued(pairRunner({ name: name.trim(), description: description.trim() }))} — UNVERIFIED
- **src/screens/app/Runners.tsx:147** **Button** + Pair new runner — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/Runners.tsx:170** **Button** Remove — onClick={() => removeRunner(runner.id)} — UNVERIFIED

### src/screens/app/Runs.tsx

- **src/screens/app/Runs.tsx:49** **button** <div className="min-w-0"> <p className="truncate text-[13px] font-semibold">{run.agent}</p> <p className="text-xs text-fg-muted"> {run.source} · {new Date(run.s — onClick={() => setOpenId(open ? null : run.id)} — UNVERIFIED

### src/screens/app/ScheduledJobs.tsx

- **src/screens/app/ScheduledJobs.tsx:24** **select** {options.map((option) => ( <option key={option} value={option}> {option} </option> ))} — onChange={(event) => onChange(event.target.value)} — UNVERIFIED
- **src/screens/app/ScheduledJobs.tsx:76** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search name, target, tenant..." — UNVERIFIED
- **src/screens/app/ScheduledJobs.tsx:105** **Button** Run now — onClick={() => recordRun({ agent: job.name, source: 'Scheduled Jobs' })} — UNVERIFIED
- **src/screens/app/ScheduledJobs.tsx:112** **Button** {job.active ? 'Pause' : 'Resume'} — onClick={() => toggleSchedule(job.id)} — UNVERIFIED
- **src/screens/app/ScheduledJobs.tsx:115** **Button** Remove — onClick={() => removeSchedule(job.id)} — UNVERIFIED

### src/screens/app/VibeStudio.tsx

- **src/screens/app/VibeStudio.tsx:37** **button** <span aria-hidden className="text-[13px]"> {item.icon} </span> {item.label} — onClick={() => setActive(item.id)} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:64** **select** {vibeModels.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setModel(event.target.value)} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:82** **button** <span aria-hidden className="grid h-10 w-10 place-items-center rounded-xl bg-accent-muted text-[15px] text-accent" > {capability.icon} </span> <p className="mt- — onClick={onPick} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:148** **form** <textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} rows={2} aria-label="Describe what to build" placeholder={vibePromptPlaceholder} cl — onSubmit={(event) => { event.preventDefault() build() }} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:155** **textarea**  — onChange={(event) => setPrompt(event.target.value)}; placeholder={vibePromptPlaceholder} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:168** **Button** → — type="submit"; disabled={!prompt.trim()} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:207** **Button** {built.status === 'published' ? `Published · v${built.version}` : 'Publish to tenant'} — onClick={() => publishArtifact(built.id)}; disabled={built.status === 'published'} — UNVERIFIED
- **src/screens/app/VibeStudio.tsx:210** **Button** Build another — onClick={() => setPhase('idle')} — UNVERIFIED

### src/screens/app/assets/AssetManagement.tsx

- **src/screens/app/assets/AssetManagement.tsx:66** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => select(item.id)} — UNVERIFIED

### src/screens/app/assets/panes.tsx

- **src/screens/app/assets/panes.tsx:206** **select** <option>All types</option> {types.map((item) => ( <option key={item.value} value={item.value}> {item.label} </option> ))} — onChange={(event) => setType(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:219** **select** <option>All projects</option> {projects.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setProject(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:233** **button** <Icon name="download" size={15} /> Export — onClick={exportCsv} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:241** **input**  — type="file"; onChange={importCsv} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:243** **button** <Icon name="bag" size={15} /> Receive from purchase — onClick={() => setNote('No purchase receipts are waiting. Assets appear here once a PO is received.')} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:249** **Button** + New asset — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:257** **button** {item} <span className="text-fg-muted">{countFor(item)}</span> — onClick={() => setStage(item)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:274** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search..." — UNVERIFIED
- **src/screens/app/assets/panes.tsx:307** **select** {assetStages.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => updateAsset(asset.id, { stage: event.target.value })} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:328** **button** 🗑 — onClick={() => removeAsset(asset.id)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:359** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, name: event.target.value }))}; placeholder="MacBook Pro 14" — UNVERIFIED
- **src/screens/app/assets/panes.tsx:369** **select** {types.map((item) => ( <option key={item.value} value={item.value}> {item.label} </option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, type: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:383** **select** {assetStages.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, stage: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:395** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, project: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:403** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, purchase: event.target.value }))}; placeholder="PO-1042" — UNVERIFIED
- **src/screens/app/assets/panes.tsx:412** **input**  — type="date"; onChange={(event) => setDraft((prev) => ({ ...prev, warrantyEnd: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:422** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:425** **Button** Create asset — disabled={!draft.name.trim()}; onClick={() => { addAsset({ ...draft, name: draft.name.trim(), assignedTo: null }) setDraft({ ...draft, name: '', project: '', purchase: '', warrantyEnd: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:512** **select** <option>All types</option> {requestTypes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setType(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:523** **select** <option>All statuses</option> {requestStatuses.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setStatus(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:534** **Button** + New request — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:542** **button** {item} — onClick={() => setScope(item)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:565** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search reference, type, person or..." — UNVERIFIED
- **src/screens/app/assets/panes.tsx:616** **Button** Approve — onClick={() => decideAssetRequest(request.id, 'Approved', me)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:623** **Button** Reject — onClick={() => decideAssetRequest(request.id, 'Rejected', me)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:654** **select** {requestTypes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, type: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:666** **textarea**  — onChange={(event) => setDraft((prev) => ({ ...prev, detail: event.target.value }))} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:675** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:678** **Button** Raise request — disabled={!draft.detail.trim()}; onClick={() => { addAssetRequest({ type: draft.type, status: 'Pending', raisedBy: me, detail: draft.detail.trim(), }) setDraft({ ...draft, detail: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:738** **button** {item} — onClick={() => setTab(item)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:758** **select** <option>By asset type</option> <option>By project</option> — onChange={(event) => setGroupBy(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:768** **button** <Icon name="download" size={15} /> Export CSV — onClick={exportCsv} — UNVERIFIED
- **src/screens/app/assets/panes.tsx:774** **button** Excel — onClick={exportCsv} — UNVERIFIED

### src/screens/app/assets/settings.tsx

- **src/screens/app/assets/settings.tsx:27** **Button** <Icon name="sparkles" size={15} /> Open Copilot — onClick={() => document.querySelector<HTMLButtonElement>('[aria-label="Open AI Copilot"]')?.click()} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:56** **Button** Save — disabled={!dirty}; onClick={() => updateAssetSettings({ approvalLevels: draft })} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:86** **select** {approvalTypes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === level.id ? { ...item, type: event.target.value } : item)), ) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:106** **button** ✕ — onClick={() => setDraft((prev) => prev.map((item) => item.id === level.id ? { ...item, approvers: item.approvers.filter((row) => row !== approver) } : item, ), ) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:122** **select** <option value="">Add…</option> {approverRoles.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => { const value = event.target.value if (!value) return setDraft((prev) => prev.map((item) => item.id === level.id && !item.approvers.includes(value) ? { ...item, approvers: [...item.approve — UNVERIFIED
- **src/screens/app/assets/settings.tsx:147** **button** 🗑 — onClick={() => setDraft((prev) => prev.filter((item) => item.id !== level.id))} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:162** **button** + Add level — onClick={() => setDraft((prev) => [ ...prev, { id: crypto.randomUUID(), level: prev.length + 1, type: 'By Role', approvers: [] }, ]) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:193** **Button** + Add Field — onClick={() => updateAssetSettings({ customFields: [...fields, { id: crypto.randomUUID(), label: 'New field', type: 'text' }], }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:209** **input**  — onChange={(event) => updateAssetSettings({ customFields: fields.map((item) => item.id === field.id ? { ...item, label: event.target.value } : item, ), }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:220** **select** {['text', 'number', 'date', 'select'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => updateAssetSettings({ customFields: fields.map((item) => item.id === field.id ? { ...item, type: event.target.value } : item, ), }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:236** **button** 🗑 — onClick={() => updateAssetSettings({ customFields: fields.filter((item) => item.id !== field.id) })} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:299** **input**  — type="checkbox"; onChange={(event) => updateAssetSettings({ agents: { ...controls, pauseAll: event.target.checked } }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:310** **input**  — type="checkbox"; onChange={(event) => updateAssetSettings({ agents: { ...controls, autoRunOnNew: event.target.checked } }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:322** **input**  — onChange={(event) => updateAssetSettings({ agents: { ...controls, autoPauseAt: event.target.value } }) }; placeholder="off" — UNVERIFIED
- **src/screens/app/assets/settings.tsx:338** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search agents by name..." — UNVERIFIED
- **src/screens/app/assets/settings.tsx:346** **select** {['All triggers', 'Automatic', 'Manual'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setTrigger(event.target.value)} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:373** **button** <span className={`block h-5 w-5 rounded-full bg-white transition-transform ${off ? '' : 'translate-x-5'}`} /> — onClick={() => updateAssetSettings({ agents: { ...controls, disabled: off ? controls.disabled.filter((item) => item !== agent.name) : [...controls.disabled, agent.name], }, }) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:437** **input**  — type="checkbox"; onChange={(event) => setShowReverted(event.target.checked)} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:445** **Button** <Icon name="refresh" size={15} /> Reset to Default — disabled={!entries.length}; onClick={resetAssetSettings} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:492** **button** <Icon name={item.icon} size={16} /> <span className="flex-1">{item.label}</span> {item.external && <Icon name="external-link" size={13} className="text-fg-muted — onClick={() => (item.external ? onOpenMasters() : setSection(item.id))} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:529** **button** <span aria-hidden>‹</span> Back to Settings — onClick={onBack} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:541** **Button** <Icon name="file-text" size={15} /> Save — disabled={!dirty}; onClick={() => updateAssetSettings({ taxonomies: draft })} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:556** **button** {item.label} <span className="text-[12px] text-fg-muted">{(draft[item.id] ?? []).length}</span> — onClick={() => setActive(item.id)} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:601** **input**  —  — UNVERIFIED
- **src/screens/app/assets/settings.tsx:609** **input**  — onChange={(event) => setEntries( entries.map((item, i) => (i === index ? { ...item, label: event.target.value } : item)), ) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:621** **input**  — onChange={(event) => setEntries( entries.map((item, i) => i === index ? { ...item, tagPrefix: event.target.value } : item, ), ) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:636** **input**  — type="checkbox"; onChange={(event) => setEntries( entries.map((item, i) => i === index ? { ...item, hasConfig: event.target.checked } : item, ), ) } — UNVERIFIED
- **src/screens/app/assets/settings.tsx:655** **button** 🗑 — onClick={() => setEntries(entries.filter((_, i) => i !== index))} — UNVERIFIED
- **src/screens/app/assets/settings.tsx:676** **button** <Icon name="plus" size={14} /> Add entry — onClick={() => setEntries([...entries, { value: '', label: 'New entry', tagPrefix: '' }])} — UNVERIFIED

### src/screens/app/business/Chat.tsx

- **src/screens/app/business/Chat.tsx:71** **select** {chatModels.map((item) => ( <option key={item} value={item}> {item} </option> ))} — onChange={(event) => setModel(event.target.value)} — UNVERIFIED
- **src/screens/app/business/Chat.tsx:83** **button** <Icon name="settings" size={15} /> System <span aria-hidden className="text-fg-muted"> ⌄ </span> — onClick={() => setSystemOpen((prev) => !prev)} — UNVERIFIED
- **src/screens/app/business/Chat.tsx:94** **Button** + New Chat — onClick={() => { setMessages([]) setDraft('') composer.current?.focus() }} — UNVERIFIED
- **src/screens/app/business/Chat.tsx:113** **textarea**  — onChange={(event) => setSystem(event.target.value)}; placeholder="Set the assistant's role, tone and constraints for this conversation." — UNVERIFIED
- **src/screens/app/business/Chat.tsx:164** **button** {suggestion} — onClick={() => void send(suggestion)} — UNVERIFIED
- **src/screens/app/business/Chat.tsx:179** **textarea**  — onChange={(event) => setDraft(event.target.value)}; placeholder="Type your message... (Shift+Enter for new line)" — UNVERIFIED
- **src/screens/app/business/Chat.tsx:189** **button** ➤ — onClick={() => void send(draft)}; disabled={!draft.trim() || thinking} — UNVERIFIED

### src/screens/app/business/Tool.tsx

- **src/screens/app/business/Tool.tsx:58** **textarea**  — onChange={(event) => setInput(event.target.value)}; placeholder={`Describe what ${tool.name} should produce.`} — UNVERIFIED
- **src/screens/app/business/Tool.tsx:67** **Button** Run · {tool.cost} credits — onClick={run}; disabled={!input.trim()} — UNVERIFIED

### src/screens/app/crm/panels.tsx

- **src/screens/app/crm/panels.tsx:77** **Action** + New deal — onClick={() => deals.create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:80** **Action** ◎ New lead — onClick={() => leads.create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:81** **Action** ☑ New task — onClick={() => tasks.create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:201** **Chip** {item} — onClick={() => setStatus(item)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:208** **Segmented**  — onChange={setView} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:209** **SearchBox**  — placeholder="Search leads..."; onChange={setQuery} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:210** **Select**  — onChange={setLimit} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:214** **Action** Show highest scores — onClick={() => setByScore(true)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:215** **Action** + New lead — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:222** **Select**  — onChange={setSource} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:223** **Select**  — onChange={setScore} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:224** **button** Sort by score — onClick={() => setByScore((prev) => !prev)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:249** **Action** + New lead — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:256** **button** Previous — disabled={currentPage === 0}; onClick={() => setPage(currentPage - 1)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:258** **button** Next — disabled={currentPage + 1 >= pageCount}; onClick={() => setPage(currentPage + 1)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:289** **SearchBox**  — placeholder="Search contacts..."; onChange={setQuery} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:290** **Action** ⇄ Find duplicates {duplicates.length ? ` (${duplicates.length})` : ''} — onClick={() => setType('All')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:293** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:297** **Action** ⤒ Import CSV — onClick={importCsv} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:298** **Action** ⤓ Export CSV — onClick={() => exportCsv('contacts')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:299** **Action** + Add contact — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:307** **Chip** {item} — onClick={() => setType(item)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:312** **Action** {archived ? '▤ Hide archived' : '▤ Include archived'} — onClick={() => setArchived((prev) => !prev)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:326** **Action** + Add contact — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:354** **Action** Prospects only — onClick={() => create('Prospect company')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:355** **Action** Needs Sales Customer — onClick={() => create('Sales customer')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:356** **SearchBox**  — placeholder="Search companies..."; onChange={setQuery} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:357** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:358** **Action** ⤒ Import — onClick={importCsv} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:359** **Action** ⤓ CSV — onClick={() => exportCsv('companies')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:360** **Action** ⤓ CSV (spreadsheet) — onClick={() => exportCsv('companies')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:361** **Action** + Add Company — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:375** **Action** + Add Company — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:399** **Segmented**  — onChange={setView} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:400** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:401** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:402** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:403** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:404** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:405** **Action** {favourites ? '★ All deals' : '★ Favorites'} — onClick={() => setFavourites((prev) => !prev)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:406** **SearchBox**  — placeholder="Search deals..."; onChange={setQuery} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:407** **Action** + New deal — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:488** **button** ‹ — onClick={() => shift(-1)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:491** **button** Today — onClick={() => setCursor(new Date())} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:497** **button** › — onClick={() => shift(1)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:501** **Action** + New Event — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:563** **SearchBox**  — placeholder="Search activities..."; onChange={setQuery} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:564** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:565** **Select**  —  — UNVERIFIED
- **src/screens/app/crm/panels.tsx:567** **Segmented**  — onChange={setView} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:568** **Action** + Log Activity — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:582** **Action** Log activity — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:607** **Action** + New sequence — onClick={() => create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:700** **Segmented**  — onChange={setFunnel} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:791** **Segmented**  — onChange={setPeriod} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:814** **input**  — placeholder="Search settings..." — UNVERIFIED
- **src/screens/app/crm/panels.tsx:832** **button** <span> {group.label} <span className="ml-1 text-fg-muted/70">{group.count}</span> </span> <span aria-hidden>{expanded ? '⌄' : '›'}</span> — onClick={() => setOpen(expanded ? null : group.id)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:847** **button** <span aria-hidden className="w-4 shrink-0 text-center text-[13px]"> {item.icon} </span> {item.label} — onClick={() => setActive(item.label)} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:879** **Action** + New pipeline — onClick={() => pipelines.create()} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:896** **button** ⧉ — onClick={() => pipelines.create('Duplicate of Sales Pipeline')} — UNVERIFIED
- **src/screens/app/crm/panels.tsx:904** **button** ✎ — onClick={() => setRenaming((prev) => !prev)} — UNVERIFIED

### src/screens/app/crm/records.tsx

- **src/screens/app/crm/records.tsx:166** **input**  — type="file"; onChange={(event) => { const file = event.target.files?.[0] if (file) void readCsv(file) event.target.value = '' }} — UNVERIFIED

### src/screens/app/dashboardPanels.tsx

- **src/screens/app/dashboardPanels.tsx:70** **button** {range} — onClick={() => onChange(range)} — UNVERIFIED
- **src/screens/app/dashboardPanels.tsx:84** **input**  — type="date" — UNVERIFIED
- **src/screens/app/dashboardPanels.tsx:88** **input**  — type="date" — UNVERIFIED

### src/screens/app/hr/AttendanceCalendar.tsx

- **src/screens/app/hr/AttendanceCalendar.tsx:53** **select** {[select, ...employees.map((item) => item.title)].map((option) => ( <option key={option}>{option}</option> ))} — onChange={(event) => setWho(event.target.value)} — UNVERIFIED
- **src/screens/app/hr/AttendanceCalendar.tsx:67** **button** ‹ — onClick={() => shift(-1)} — UNVERIFIED
- **src/screens/app/hr/AttendanceCalendar.tsx:74** **button** › — onClick={() => shift(1)} — UNVERIFIED
- **src/screens/app/hr/AttendanceCalendar.tsx:81** **button** today — onClick={() => setCursor(new Date())}; disabled={isThisMonth} — UNVERIFIED
- **src/screens/app/hr/AttendanceCalendar.tsx:96** **button** {option} — onClick={() => setView(option)} — UNVERIFIED

### src/screens/app/hr/panels.tsx

- **src/screens/app/hr/panels.tsx:101** **button** Remove — onClick={() => removeAppRecord(key, item.id)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:117** **textarea**  — onChange={(event) => setDraft(event.target.value)}; placeholder={`${placeholder} — one per line`} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:126** **input**  — onChange={(event) => setDraft(event.target.value)}; placeholder={placeholder} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:134** **button** + Add — onClick={commit}; disabled={!draft.trim()} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:141** **button** {bulk ? 'Single add' : 'Bulk add'} — onClick={() => setBulk((prev) => !prev)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:161** **button** <span aria-hidden className="w-4 shrink-0 text-center text-[13px]"> {item.icon} </span> {item.label} — onClick={() => setActive(item.id)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:232** **button** {tab.icon && <span aria-hidden className="text-[13px]">{tab.icon}</span>} {tab.label} — onClick={() => onChange(tab.label)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:337** **button** {item.icon && <span aria-hidden className="text-[13px]">{item.icon}</span>} {item.label} — onClick={() => setMode(item.label)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:361** **Action** {item.label} — onClick={() => open(item.label, item.primary ? dialog : undefined)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:395** **Action** {page.notice.action} — onClick={() => open(page.notice!.action)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:409** **Select**  —  — UNVERIFIED
- **src/screens/app/hr/panels.tsx:411** **Select**  —  — UNVERIFIED
- **src/screens/app/hr/panels.tsx:417** **Action** {subToolbar.secondaryAction} — onClick={() => open(subToolbar.secondaryAction!)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:420** **Action** {subToolbar.action} — onClick={() => open(subToolbar.action!, activeSubTab?.modal)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:441** **Chip** {item} — onClick={() => setChip(item)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:447** **Action** {page.chipsAction} — onClick={() => open(page.chipsAction!, dialog)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:458** **input**  — type="date" — UNVERIFIED
- **src/screens/app/hr/panels.tsx:465** **input**  — type="date" — UNVERIFIED
- **src/screens/app/hr/panels.tsx:473** **Select**  —  — UNVERIFIED
- **src/screens/app/hr/panels.tsx:476** **button** {page.dateFilterCard.export} — onClick={exportCsv}; disabled={!exportRows.length} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:511** **SearchBox**  — placeholder={page.search}; onChange={setQuery} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:513** **Select**  —  — UNVERIFIED
- **src/screens/app/hr/panels.tsx:518** **Action** {page.action} — onClick={() => open(page.action!, dialog)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:530** **input**  — type="date" — UNVERIFIED
- **src/screens/app/hr/panels.tsx:538** **input**  — type="date" — UNVERIFIED
- **src/screens/app/hr/panels.tsx:555** **Select**  —  — UNVERIFIED
- **src/screens/app/hr/panels.tsx:558** **Action** {page.action} — onClick={() => open(page.action!, dialog)} — UNVERIFIED
- **src/screens/app/hr/panels.tsx:597** **button** {option === 'list' ? '☰' : '▦'} — onClick={() => onChange(option)} — UNVERIFIED

### src/screens/app/pitch/BrandKit.tsx

- **src/screens/app/pitch/BrandKit.tsx:41** **Button** <Icon name="file-text" size={15} /> Save — disabled={!dirty}; onClick={() => updateBrandKit(draft)} — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:55** **input**  — onChange={(event) => set('firmName', event.target.value)}; placeholder="Bright Path Digital" — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:78** **input**  — type="file"; onChange={(event) => { const file = event.target.files?.[0] if (!file) return // Held as a data URL so the preview survives a reload. const reader = new FileReader() reader.onload = () => set('logoUrl', String — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:92** **input**  — onChange={(event) => set('logoUrl', event.target.value)}; placeholder="…or paste an https:// URL" — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:134** **input**  — type="color"; onChange={(event) => set(swatch.key, event.target.value)} — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:145** **input**  — onChange={(event) => set(swatch.key, event.target.value)} — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:164** **input**  — onChange={(event) => set('contactName', event.target.value)}; placeholder="Mira Kapoor" — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:173** **input**  — onChange={(event) => set('contactTitle', event.target.value)}; placeholder="Agency Principal" — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:182** **input**  — onChange={(event) => set('contactEmail', event.target.value)}; placeholder="e.g. mira@brightpath.in" — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:191** **input**  — onChange={(event) => set('contactPhone', event.target.value)}; placeholder="e.g. +91 98..." — UNVERIFIED
- **src/screens/app/pitch/BrandKit.tsx:214** **Button** {pitchSampleLoaded ? 'Clear sample data' : 'Load sample data'} — onClick={() => (pitchSampleLoaded ? clearPitchSample() : loadPitchSample())} — UNVERIFIED

### src/screens/app/pitch/panes.tsx

- **src/screens/app/pitch/panes.tsx:60** **Button** + New RFP —  — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:186** **button** <Icon name="download" size={15} /> Export — onClick={exportCsv}; disabled={!visible.length} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:194** **Button** + New RFP —  — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:202** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search prospect or summary..." — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:210** **select** <option>All stages</option> {rfpStages.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setStage(event.target.value)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:221** **select** <option>All verticals</option> {verticals.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setVertical(event.target.value)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:315** **input**  — type="file"; onChange={(event) => setFile(event.target.files?.[0] ?? null)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:328** **input**  — onChange={(event) => setProspect(event.target.value)}; placeholder="e.g. City of Hidden Hills" — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:344** **button** {item.id === 'auto' && <span className="block text-[15px]">✨</span>} <span className="mt-1 block text-[14px] font-semibold">{item.name}</span> <span className=" — onClick={() => setSchema(item.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:361** **Button** Cancel — onClick={() => setFile(null)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:364** **Button** <Icon name="sparkles" size={15} /> Extract &amp; Open ▶ — disabled={!file}; onClick={extract} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:393** **Button** + Add document — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:399** **button** All ( {countFor('all')} ) — onClick={() => setCategory('all')} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:409** **button** <span aria-hidden>{item.icon}</span> {item.label} ( {countFor(item.id)} ) — onClick={() => setCategory(item.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:435** **Button** Remove — onClick={() => removeKbDoc(doc.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:445** **Button** <Icon name="download" size={15} className="rotate-180" /> Upload your first document — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:457** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, title: event.target.value }))} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:465** **select** {kbCategories.map((item) => ( <option key={item.id} value={item.id}> {item.label} </option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, category: event.target.value }))} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:479** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:482** **Button** Add document — disabled={!draft.title.trim()}; onClick={() => { addKbDoc({ title: draft.title.trim(), category: draft.category }) setDraft({ ...draft, title: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:525** **input**  — type="file"; onChange={upload} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:547** **Button** Make default — onClick={() => setDefaultTemplate(template.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:555** **Button** Remove — onClick={() => removePitchTemplate(template.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:575** **input**  — type="file"; onChange={upload} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:615** **button** + New custom schema — onClick={() => { addCustomSchema({ name: `Custom schema ${customSchemas.length + 1}`, fields: ['Client name'] }) setSelected(null) }} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:628** **button** {item.name} <span className="text-[12px] text-fg-muted">{item.fields.length}</span> — onClick={() => setSelected(item.id)} — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:658** **input**  — onChange={(event) => updateCustomSchema(schema.id, { fields: schema.fields.map((item, i) => (i === index ? event.target.value : item)), }) } — UNVERIFIED
- **src/screens/app/pitch/panes.tsx:673** **Button** + Add field — onClick={() => updateCustomSchema(schema.id, { fields: [...schema.fields, 'New field'] })} — UNVERIFIED

### src/screens/app/pos/SalesPos.tsx

- **src/screens/app/pos/SalesPos.tsx:135** **Button** <Icon name="refresh" size={15} /> Returns (RMA) — onClick={() => select('sales_returns')} — UNVERIFIED
- **src/screens/app/pos/SalesPos.tsx:141** **input**  — placeholder="Scan receipt or type invoice no..." — UNVERIFIED
- **src/screens/app/pos/SalesPos.tsx:146** **select** {paymentFilters.map((item) => ( <option key={item}>{item}</option> ))} —  — UNVERIFIED
- **src/screens/app/pos/SalesPos.tsx:154** **select** {sourceFilters.map((item) => ( <option key={item}>{item}</option> ))} —  — UNVERIFIED
- **src/screens/app/pos/SalesPos.tsx:218** **Button** <Icon name="refresh" size={15} /> Run due now — disabled={!dueSubscriptions.length}; onClick={runSubscriptionSweep} — UNVERIFIED

### src/screens/app/pos/panes.tsx

- **src/screens/app/pos/panes.tsx:87** **button** {item} — onClick={() => setPeriod(item)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:332** **button** <Icon name="download" size={15} /> Export — onClick={exportCsv} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:340** **input**  — type="file"; onChange={importCsv} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:342** **Button** + New customer — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:352** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search by name, email, tax ID..." — UNVERIFIED
- **src/screens/app/pos/panes.tsx:361** **input**  — type="checkbox"; onChange={(event) => setShowInactive(event.target.checked)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:407** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, [key]: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:416** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:419** **Button** Create customer — disabled={!draft.name.trim()}; onClick={() => { addPosCustomer({ ...draft, name: draft.name.trim(), active: true }) setDraft({ name: '', email: '', taxId: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:472** **button** {item} — onClick={() => setStatus(item)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:485** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search number / title..." — UNVERIFIED
- **src/screens/app/pos/panes.tsx:493** **Button** + New quotation — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:524** **select** <option value="">Select a customer…</option> {posCustomers.map((item) => ( <option key={item.id}>{item.name}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, customer: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:535** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, customer: event.target.value }))}; placeholder="No customers yet — type a name" — UNVERIFIED
- **src/screens/app/pos/panes.tsx:545** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, title: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:553** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, total: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:563** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:566** **Button** Create quotation — disabled={!draft.customer.trim()}; onClick={() => { addPosDoc({ kind: 'quotation', customer: draft.customer.trim(), status: 'Draft', total: Number(draft.total) || 0, source: 'Manual', payment: 'Unpaid', reason: draft.title.trim(), dueDate: '', — UNVERIFIED
- **src/screens/app/pos/panes.tsx:629** **input**  — type="number"; onChange={(event) => setCounted(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:636** **Button** Close shift — disabled={counted === ''}; onClick={() => closePosShift(openShift.id, Number(counted))} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:660** **input**  — type="number"; onChange={(event) => setFloat(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:677** **select** <option>— no inventory leg —</option> <option>Main warehouse</option> <option>Store front</option> — onChange={(event) => setWarehouse(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:689** **input**  — onChange={(event) => setNotes(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:692** **Button** Open shift — onClick={() => openPosShift({ cashier: session?.user.fullName ?? 'Cashier', openingFloat: Number(float) || 0, currency: 'USD', warehouse, notes, }) } — UNVERIFIED
- **src/screens/app/pos/panes.tsx:759** **button** {item} — onClick={() => setPeriod(item)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1011** **button** <Icon name={item.icon} size={15} /> {item.label} — onClick={() => setTab(item.id)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1026** **Select**  — onChange={setStatus} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1077** **Button** <Icon name="file-text" size={15} /> Save — disabled={!dirty}; onClick={() => updateMatchSettings(draft)} — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1108** **input**  — type="number"; onChange={(event) => row.key === 'price' ? set('priceTolerance', Number(event.target.value)) : set('quantityTolerance', Number(event.target.value)) } — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1121** **select** {toleranceActions.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => row.key === 'price' ? set('priceAction', event.target.value) : set('quantityAction', event.target.value) } — UNVERIFIED
- **src/screens/app/pos/panes.tsx:1167** **input**  — type="checkbox"; onChange={(event) => set(row.key, event.target.checked)} — UNVERIFIED

### src/screens/app/pos/parts.tsx

- **src/screens/app/pos/parts.tsx:69** **select** {options.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => onChange(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:128** **Button** + {createLabel} — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:137** **Select**  — onChange={setStatus} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:149** **Button** Delete — onClick={() => removePosDoc(doc.id)} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:166** **select** <option value="">Select a customer…</option> {posCustomers.map((item) => ( <option key={item.id}>{item.name}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, customer: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:177** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, customer: event.target.value }))}; placeholder="No customers yet — type a name" — UNVERIFIED
- **src/screens/app/pos/parts.tsx:187** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, total: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:198** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, reason: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:207** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/parts.tsx:210** **Button** Create — disabled={!draft.customer.trim()}; onClick={() => { addPosDoc({ kind, customer: draft.customer.trim(), status: statuses[1] ?? 'Draft', total: Number(draft.total) || 0, source: 'Manual', payment: 'Unpaid', reason: draft.reason.trim(), dueDate:  — UNVERIFIED

### src/screens/app/pos/settings.tsx

- **src/screens/app/pos/settings.tsx:71** **Button** <Icon name="sparkles" size={14} className="mr-1.5 inline align-[-2px]" /> Add standard GST slabs — onClick={seedSlabs} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:75** **Button** + New category — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:97** **button** <Icon name="trash" size={15} /> — onClick={() => updatePosSettings( { taxCategories: rows.filter((item) => item.id !== row.id) }, `Deleted tax category ${row.name}`, ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:116** **Button** <Icon name="sparkles" size={14} className="mr-1.5 inline align-[-2px]" /> Add the standard GST slabs (0 / 5 / 12 / 18 / 28%) — onClick={seedSlabs} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:130** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, name: event.target.value }))}; placeholder="GST 18%" — UNVERIFIED
- **src/screens/app/pos/settings.tsx:139** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, rate: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:152** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:155** **Button** Create — disabled={!draft.name.trim()}; onClick={() => { const rate = Number(draft.rate) || 0 updatePosSettings( { taxCategories: [ ...rows, { id: id(), name: draft.name.trim(), rate, withinState: rate === 0 ? 'Exempt' : `CGST ${rate / 2}% + SGST $ — UNVERIFIED
- **src/screens/app/pos/settings.tsx:208** **Button** New group — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:216** **button** <Icon name="trash" size={15} /> — onClick={() => updatePosSettings( { customerGroups: rows.filter((item) => item.id !== row.id) }, `Deleted customer group ${row.name}`, ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:238** **Button** <Icon name="sparkles" size={14} className="mr-1.5 inline align-[-2px]" /> Add Walk-in / Regulars / Wholesale — onClick={() => updatePosSettings( { customerGroups: standardCustomerGroups.map((item) => ({ id: id(), name: item })) }, 'Added the Walk-in / Regulars / Wholesale groups', ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:250** **Button** New group — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:262** **input**  — onChange={(event) => setName(event.target.value)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:265** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:268** **Button** Create — disabled={!name.trim()}; onClick={() => { updatePosSettings( { customerGroups: [...rows, { id: id(), name: name.trim() }] }, `Created customer group ${name.trim()}`, ) setName('') setOpen(false) }} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:308** **Button** + New programme — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:326** **button** <Icon name="trash" size={15} /> — onClick={() => updatePosSettings( { loyalty: rows.filter((item) => item.id !== row.id) }, `Deleted loyalty programme ${row.name}`, ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:351** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, name: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:359** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, pointsPer: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:370** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, pointValue: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:381** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:384** **Button** Create — disabled={!draft.name.trim()}; onClick={() => { updatePosSettings( { loyalty: [ ...rows, { id: id(), name: draft.name.trim(), pointsPer: Number(draft.pointsPer) || 0, pointValue: Number(draft.pointValue) || 0, tiers: [], }, ], }, `Created  — UNVERIFIED
- **src/screens/app/pos/settings.tsx:477** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, [field.key]: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:498** **Button** Save — disabled={!dirty || !valid}; onClick={() => updatePosSettings({ cashVariance: parsed }, 'Updated cash variance tolerance')} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:505** **Button** <Icon name="refresh" size={14} className="mr-1.5 inline align-[-2px]" /> Restore defaults — onClick={() => { setDraft(Object.fromEntries(Object.entries(varianceDefaults).map(([k, v]) => [k, String(v)]))) updatePosSettings({ cashVariance: varianceDefaults }, 'Restored the default cash variance tolera — UNVERIFIED
- **src/screens/app/pos/settings.tsx:534** **Button** + Add Field — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:545** **button** <Icon name="trash" size={15} /> — onClick={() => updatePosSettings( { customFields: rows.filter((item) => item.id !== row.id) }, `Deleted custom field ${row.label}`, ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:573** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, label: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:581** **select** {customFieldTypes.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, type: event.target.value }))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:593** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:596** **Button** Add — disabled={!draft.label.trim()}; onClick={() => { updatePosSettings( { customFields: [...rows, { id: id(), label: draft.label.trim(), type: draft.type }] }, `Added custom field ${draft.label.trim()}`, ) setDraft({ label: '', type: customFiel — UNVERIFIED
- **src/screens/app/pos/settings.tsx:645** **input**  — type="checkbox"; onChange={(event) => setAgents( { pauseAll: event.target.checked }, event.target.checked ? 'Paused AI agents across all apps' : 'Resumed AI agents', ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:659** **input**  — type="checkbox"; onChange={(event) => setAgents( { autoRunOnNew: event.target.checked }, event.target.checked ? 'Agents auto-run on new records' : 'Agents now run manually', ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:674** **input**  — onChange={(event) => setAgents({ autoPauseAt: event.target.value }, 'Changed the auto-pause threshold')}; placeholder="off" — UNVERIFIED
- **src/screens/app/pos/settings.tsx:701** **button** <span className={`block h-5 w-5 rounded-full bg-white shadow-sm transition-transform ${ off ? '' : 'translate-x-5' }`} /> — onClick={() => setAgents( { disabled: off ? controls.disabled.filter((item) => item !== agent.name) : [...controls.disabled, agent.name], }, `${off ? 'Enabled' : 'Disabled'} ${agent.name}`, ) } — UNVERIFIED
- **src/screens/app/pos/settings.tsx:766** **input**  — type="checkbox"; onChange={(event) => setShowReverted(event.target.checked)} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:774** **Button** <Icon name="refresh" size={14} className="mr-1.5 inline align-[-2px]" /> Reset to Default — disabled={!changes.length}; onClick={() => updatePosSettings( { taxCategories: [], customerGroups: [], loyalty: [], cashVariance: varianceDefaults, customFields: [], agents: { pauseAll: false, autoRunOnNew: true, autoPauseAt: '', disabl — UNVERIFIED
- **src/screens/app/pos/settings.tsx:879** **Button** <Icon name="sparkles" size={14} className="mr-1.5 inline align-[-2px]" /> Open Copilot — onClick={() => window.dispatchEvent(new Event('apragya:copilot'))} — UNVERIFIED
- **src/screens/app/pos/settings.tsx:888** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => setTab(item.id)} — UNVERIFIED

### src/screens/app/setup/Companies.tsx

- **src/screens/app/setup/Companies.tsx:65** **input**  — onChange={(event) => setName(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:73** **input**  — onChange={(event) => setCode(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:82** **select** {currencies.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setCurrency(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:94** **select** {countries.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setCountry(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:107** **select** {entities .filter((item) => item.id !== entity?.id) .map((item) => ( <option key={item.id} value={item.id}> {item.name} ({item.currency}) </option> ))} — onChange={(event) => setParentId(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:125** **Button** Cancel — onClick={onClose} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:128** **Button** {editing ? 'Save entity' : 'Create entity'} — onClick={save}; disabled={!name.trim() || !code.trim()} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:149** **Button** + New company — onClick={() => setForm({ open: true, entity: null })} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:174** **Chip** {entity.currency} —  — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:181** **Chip** {entity.parentId ? 'Subsidiary' : 'Primary'} —  — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:184** **Chip** {entity.status} —  — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:187** **button** ✎ — onClick={() => setForm({ open: true, entity })} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:224** **Button** + New pair — disabled={!enabled}; onClick={() => { setFromId(entities[0]?.id ?? '') setToId(entities[1]?.id ?? '') setOpen(true) }} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:274** **select** {entities.map((item) => ( <option key={item.id} value={item.id}> {item.name} </option> ))} — onChange={(event) => setFromId(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:288** **select** {entities .filter((item) => item.id !== fromId) .map((item) => ( <option key={item.id} value={item.id}> {item.name} </option> ))} — onChange={(event) => setToId(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:304** **input**  — onChange={(event) => setAccount(event.target.value)}; placeholder="e.g. 3900 — Intercompany elimination" — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:313** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:316** **Button** Create pair — disabled={!fromId || !toId || fromId === toId || !account.trim()}; onClick={() => { addIntercompanyPair({ fromId, toId, eliminationAccount: account.trim() }) setAccount('') setOpen(false) }} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:355** **select** {entities.map((item) => ( <option key={item.id} value={item.id}> {item.name} ({item.currency}) </option> ))} — onChange={(event) => setParentId(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:369** **input**  — type="date"; onChange={(event) => setAsOf(event.target.value)} — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:380** **Chip** 🌐 {parent?.currency ?? 'USD'} —  — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:382** **Chip** {consolidated.length} —  — UNVERIFIED
- **src/screens/app/setup/Companies.tsx:441** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => setParams({ tab: item.id })} — UNVERIFIED

### src/screens/app/support/panes.tsx

- **src/screens/app/support/panes.tsx:33** **Button** Back to dashboard — onClick={onBack} — UNVERIFIED
- **src/screens/app/support/panes.tsx:37** **Button** <Icon name="sparkles" size={15} /> Upgrade plan —  — UNVERIFIED
- **src/screens/app/support/panes.tsx:175** **button** + Save current filter — onClick={() => setSaved(`${queue} · ${status}${source === 'All sources' ? '' : ` · ${source}`}`)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:190** **Button** + New ticket — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:197** **button** {item} — onClick={() => setQueue(item)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:210** **button** {item} — onClick={() => setStatus(item)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:221** **select** {sources.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setSource(event.target.value)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:233** **input**  — onChange={(event) => setTagFilter(event.target.value)}; placeholder="Filter by tag(s)..." — UNVERIFIED
- **src/screens/app/support/panes.tsx:246** **input**  — onChange={(event) => setQuery(event.target.value)}; placeholder="Search tickets..." — UNVERIFIED
- **src/screens/app/support/panes.tsx:261** **button** <span className="w-20 shrink-0 font-mono text-[12px] text-fg-muted">{ticket.reference}</span> <span className="min-w-[12rem] flex-1"> <span className="block tex — onClick={() => setExpanded((prev) => (prev === ticket.id ? null : ticket.id))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:287** **select** {statuses .filter((item) => item !== 'All') .map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => updateTicket(ticket.id, { status: event.target.value })} — UNVERIFIED
- **src/screens/app/support/panes.tsx:299** **Button** {ticket.assignee ? `Unassign (${ticket.assignee})` : 'Assign to me'} — onClick={() => updateTicket(ticket.id, { assignee: ticket.assignee ? null : me })} — UNVERIFIED
- **src/screens/app/support/panes.tsx:306** **Button** {ticket.slaBreached ? 'Clear SLA breach' : 'Mark SLA breached'} — onClick={() => updateTicket(ticket.id, { slaBreached: !ticket.slaBreached })} — UNVERIFIED
- **src/screens/app/support/panes.tsx:341** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, subject: event.target.value }))}; placeholder="Cannot log in to the portal" — UNVERIFIED
- **src/screens/app/support/panes.tsx:350** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, requester: event.target.value }))}; placeholder="name@customer.com" — UNVERIFIED
- **src/screens/app/support/panes.tsx:360** **select** {priorities.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, priority: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:372** **select** {categories.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, category: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:384** **select** {sources .filter((item) => item !== 'All sources') .map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, source: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:398** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, tags: event.target.value }))}; placeholder="comma, separated" — UNVERIFIED
- **src/screens/app/support/panes.tsx:409** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:412** **Button** Create ticket — disabled={!draft.subject.trim()}; onClick={() => { addTicket({ subject: draft.subject.trim(), requester: draft.requester.trim() || 'Unknown requester', status: 'New', priority: draft.priority, category: draft.category, source: draft.source, t — UNVERIFIED
- **src/screens/app/support/panes.tsx:495** **input**  — type="file"; onChange={onImport} — UNVERIFIED
- **src/screens/app/support/panes.tsx:497** **button** <Icon name="folder" size={15} /> New Category — onClick={() => setOpen('category')} — UNVERIFIED
- **src/screens/app/support/panes.tsx:503** **Button** + New Article — onClick={() => setOpen('article')} — UNVERIFIED
- **src/screens/app/support/panes.tsx:512** **input**  — onChange={(event) => setSearch(event.target.value)}; placeholder="Describe the problem in any language..." — UNVERIFIED
- **src/screens/app/support/panes.tsx:520** **Button** Search — disabled={!search.trim()}; onClick={() => setApplied(search)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:526** **select** {['All Categories', ...kbCategories].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setCategory(event.target.value)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:537** **button** {item} — onClick={() => setState(item)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:562** **Button** Delete — onClick={() => removeKbArticle(article.id)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:586** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, title: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:594** **select** {kbCategories.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, category: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:606** **select** {['draft', 'published', 'archived'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, state: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:618** **textarea**  — onChange={(event) => setDraft((prev) => ({ ...prev, body: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:627** **Button** Cancel — onClick={() => setOpen(null)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:630** **Button** Create article — disabled={!draft.title.trim()}; onClick={() => { addKbArticle({ title: draft.title.trim(), category: draft.category, state: draft.state as 'draft' | 'published' | 'archived', body: draft.body, }) setDraft({ ...draft, title: '', body: '' })  — UNVERIFIED
- **src/screens/app/support/panes.tsx:654** **input**  — onChange={(event) => setCategoryName(event.target.value)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:661** **Button** Cancel — onClick={() => setOpen(null)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:664** **Button** Create category — disabled={!categoryName.trim()}; onClick={() => { addKbCategory(categoryName.trim()) setCategoryName('') setOpen(null) }} — UNVERIFIED
- **src/screens/app/support/panes.tsx:697** **Button** <Icon name="message" size={15} /> New request — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:724** **textarea**  — onChange={(event) => setSubject(event.target.value)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:732** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:735** **Button** Submit request — disabled={!subject.trim()}; onClick={() => { addTicket({ subject: subject.trim(), requester: email, status: 'New', priority: 'Medium', category: 'Other', source: 'Portal', tags: [], assignee: null, }) setSubject('') setOpen(false) }} — UNVERIFIED
- **src/screens/app/support/panes.tsx:774** **Button** + New Response — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:788** **Button** Delete — onClick={() => removeCannedResponse(response.id)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:808** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, title: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:816** **textarea**  — onChange={(event) => setDraft((prev) => ({ ...prev, body: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/panes.tsx:825** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:828** **Button** Create response — disabled={!draft.title.trim()}; onClick={() => { addCannedResponse({ title: draft.title.trim(), body: draft.body.trim() }) setDraft({ title: '', body: '' }) setOpen(false) }} — UNVERIFIED
- **src/screens/app/support/panes.tsx:886** **button** <Icon name={item.icon} size={15} /> {item.label} — onClick={() => setTab(item.id)} — UNVERIFIED
- **src/screens/app/support/panes.tsx:901** **select** {reportRanges.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setRange(event.target.value)} — UNVERIFIED

### src/screens/app/support/settings.tsx

- **src/screens/app/support/settings.tsx:94** **button** {item} — onClick={() => setTab(item)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:112** **Button** + {copy.add} — onClick={() => { setEditing(null) setDraft({ id: crypto.randomUUID(), label: '', slug: '', behaviour: 'active', slaHours: 24 }) setOpen(true) }} — UNVERIFIED
- **src/screens/app/support/settings.tsx:180** **button** ✎ — onClick={() => { setEditing(row) setDraft(row) setOpen(true) }} — UNVERIFIED
- **src/screens/app/support/settings.tsx:191** **button** 🗑 — onClick={() => persist(rows.filter((item) => item.id !== row.id))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:211** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, label: event.target.value, // The slug follows the label until someone edits it by hand. slug: editing ? prev.slug : event.target.value.toLowerCase().replace — UNVERIFIED
- **src/screens/app/support/settings.tsx:226** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, slug: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:235** **select** {Object.entries(behaviourLabel).map(([value, label]) => ( <option key={value} value={value}> {label} </option> ))} — onChange={(event) => setDraft((prev) => ({ ...prev, behaviour: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:251** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, slaHours: Number(event.target.value) }))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:262** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:265** **Button** {editing ? 'Save' : 'Create'} — disabled={!draft.label.trim()}; onClick={() => { persist(editing ? rows.map((item) => (item.id === draft.id ? draft : item)) : [...rows, draft]) setOpen(false) }} — UNVERIFIED
- **src/screens/app/support/settings.tsx:301** **input**  — type="checkbox"; onChange={(event) => set('enabled', event.target.checked)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:317** **input**  — onChange={(e) => set('panelTitle', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:321** **input**  — onChange={(e) => set('launcherText', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:331** **input**  — onChange={(e) => set('subtitle', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:337** **input**  — onChange={(e) => set('confirmation', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:349** **input**  — type="color"; onChange={(e) => set('accent', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:356** **input**  — onChange={(e) => set('accent', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:365** **select** {widgetPositions.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(e) => set('position', e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:374** **input**  — type="checkbox"; onChange={(e) => set('showHelpTab', e.target.checked)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:383** **input**  — type="checkbox"; onChange={(e) => set('requireEmail', e.target.checked)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:396** **Button** Save widget settings — disabled={!dirty}; onClick={save} — UNVERIFIED
- **src/screens/app/support/settings.tsx:440** **Button** <Icon name="file-text" size={15} /> {copied ? 'Copied' : 'Copy snippet'} — onClick={async () => { try { await navigator.clipboard.writeText(snippet) setCopied(true) } catch { setCopied(false) } }} — UNVERIFIED
- **src/screens/app/support/settings.tsx:481** **input**  — type="checkbox"; onChange={(e) => set('sendOnResolve', e.target.checked)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:498** **input**  — type="number"; onChange={(e) => set('waitHours', Number(e.target.value))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:511** **input**  — type="number"; onChange={(e) => set('skipOlderDays', Number(e.target.value))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:524** **input**  — type="number"; onChange={(e) => set('lowScore', Number(e.target.value))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:541** **input**  — type="number"; onChange={(e) => set('slaWarnPercent', Number(e.target.value))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:559** **input**  — onChange={(e) => set('portalUrl', e.target.value)}; placeholder="https://support.yourcompany.com/portal" — UNVERIFIED
- **src/screens/app/support/settings.tsx:569** **Button** Send pending surveys now — onClick={() => setNote( pending ? `${pending} survey${pending === 1 ? '' : 's'} queued for resolved tickets without a rating.` : 'No resolved tickets are waiting on a survey.', ) } — UNVERIFIED
- **src/screens/app/support/settings.tsx:581** **Button** Save CSAT settings — disabled={!dirty}; onClick={save} — UNVERIFIED
- **src/screens/app/support/settings.tsx:644** **Button** + {addLabel} — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:660** **Button** Delete — onClick={() => updateSupportSettings({ [settingsKey]: rules.filter((item) => item.id !== rule.id), } as Partial<SupportSettings>) } — UNVERIFIED
- **src/screens/app/support/settings.tsx:684** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, name: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:692** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, detail: event.target.value }))} — UNVERIFIED
- **src/screens/app/support/settings.tsx:700** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:703** **Button** Create — disabled={!draft.name.trim()}; onClick={() => { updateSupportSettings({ [settingsKey]: [ ...rules, { id: crypto.randomUUID(), name: draft.name.trim(), detail: draft.detail.trim(), enabled: true }, ], } as Partial<SupportSettings>) setDraft — UNVERIFIED
- **src/screens/app/support/settings.tsx:758** **input**  — type="checkbox"; onChange={(event) => setShowReviewed(event.target.checked)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:821** **select** {exportReports.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(e) => setReport(e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:832** **select** {exportFormats.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(e) => setFormat(e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:843** **input**  — type="date"; onChange={(e) => setFrom(e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:851** **input**  — type="date"; onChange={(e) => setTo(e.target.value)} — UNVERIFIED
- **src/screens/app/support/settings.tsx:858** **Button** <Icon name="download" size={15} /> Download — onClick={download} — UNVERIFIED
- **src/screens/app/support/settings.tsx:868** **Button** + New schedule — onClick={() => updateSupportSettings({ reportSchedules: [ ...schedules, { id: crypto.randomUUID(), name: `${report} — weekly`, detail: `${format} every Monday`, enabled: true, }, ], }) } — UNVERIFIED
- **src/screens/app/support/settings.tsx:894** **Button** Delete — onClick={() => updateSupportSettings({ reportSchedules: schedules.filter((row) => row.id !== item.id) }) } — UNVERIFIED
- **src/screens/app/support/settings.tsx:994** **Button** <Icon name="refresh" size={15} /> Index articles — disabled={!published.length}; onClick={() => updateSupportSettings({ indexedArticles: published.map((item) => item.id) })} — UNVERIFIED
- **src/screens/app/support/settings.tsx:1062** **Button** <Icon name="alert-triangle" size={15} /> Run detector — onClick={() => { overdue.forEach((ticket) => updateTicket(ticket.id, { slaBreached: true })) setResult( overdue.length ? `Detector flagged ${overdue.length} ticket${overdue.length === 1 ? '' : 's'} past SLA.` — UNVERIFIED
- **src/screens/app/support/settings.tsx:1075** **Button** <Icon name="clock" size={15} /> Run auto-close — onClick={() => { settled.forEach((ticket) => updateTicket(ticket.id, { status: 'Closed' })) setResult( settled.length ? `Auto-close closed ${settled.length} resolved ticket${settled.length === 1 ? '' : 's'}.` — UNVERIFIED
- **src/screens/app/support/settings.tsx:1170** **button** <Icon name={item.icon} size={15} /> {item.label} — onClick={() => setTab(item.id)} — UNVERIFIED

### src/screens/app/travel/TravelExpense.tsx

- **src/screens/app/travel/TravelExpense.tsx:62** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => setParams(item.id === 'dashboard' ? {} : { tab: item.id })} — UNVERIFIED

### src/screens/app/travel/panes.tsx

- **src/screens/app/travel/panes.tsx:180** **button** <Icon name="zap" size={15} /> Quick expense — onClick={() => setOpen('quick')} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:186** **Button** + New report — onClick={() => setOpen('report')} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:207** **Button** {nextAction[report.status]} — onClick={() => advanceExpenseReport(report.id)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:233** **input**  — onChange={(event) => setTitle(event.target.value)}; placeholder={open === 'quick' ? 'Airport taxi' : 'Mumbai client visit'} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:245** **input**  — type="number"; onChange={(event) => setAmount(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:255** **select** {expenseCategories.map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setCategory(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:266** **select** {currencies.map((item) => ( <option key={item.code} value={item.code}> {item.symbol} {item.code} - {item.name} </option> ))} — onChange={(event) => setCurrency(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:277** **Button** Cancel — onClick={() => setOpen(null)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:280** **Button** Create — disabled={!title.trim()}; onClick={create} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:332** **input**  — type="file"; onChange={onFile} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:334** **button** <Icon name="sparkles" size={15} /> Auto-match — onClick={() => { const matched = autoMatchCards() setNote( expenseReports.length ? `${matched} charge${matched === 1 ? '' : 's'} matched to an expense report.` : 'Nothing to match against — no expense reports — UNVERIFIED
- **src/screens/app/travel/panes.tsx:354** **select** {['All', 'Matched', 'Unmatched'].map((item) => ( <option key={item}>{item}</option> ))} — onChange={(event) => setFilter(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:411** **Button** Create run ( {selected.length} ) — disabled={!selected.length}; onClick={() => { createReimbursementRun(selected) setSelected([]) }} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:428** **input**  — type="checkbox"; onChange={(event) => setSelected((prev) => event.target.checked ? [...prev, report.id] : prev.filter((id) => id !== report.id), ) } — UNVERIFIED
- **src/screens/app/travel/panes.tsx:454** **button** <span className="min-w-[10rem] flex-1"> <span className="block text-[13px] font-medium"> {run.reportIds.length} report{run.reportIds.length === 1 ? '' : 's'} </ — onClick={() => advanceReimbursementRun(run.id)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:499** **Button** + New request — onClick={() => setOpen(true)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:506** **button** {item} — onClick={() => setScope(item)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:539** **Button** Close — onClick={() => closeTravelRequest(request.id)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:560** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, purpose: event.target.value }))}; placeholder="Client visit - Mumbai" — UNVERIFIED
- **src/screens/app/travel/panes.tsx:573** **input**  — type="date"; onChange={(event) => setDraft((prev) => ({ ...prev, start: event.target.value }))} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:584** **input**  — type="date"; onChange={(event) => setDraft((prev) => ({ ...prev, end: event.target.value }))} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:593** **select** <option value="domestic">domestic</option> <option value="international">international</option> — onChange={(event) => setDraft((prev) => ({ ...prev, type: event.target.value as typeof prev.type })) } — UNVERIFIED
- **src/screens/app/travel/panes.tsx:606** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, estimatedCost: event.target.value }))} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:616** **select** <option value="">- None -</option> — onChange={(event) => setDraft((prev) => ({ ...prev, project: event.target.value }))} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:626** **select** <option>- Select -</option> — disabled=true — UNVERIFIED
- **src/screens/app/travel/panes.tsx:634** **Button** Cancel — onClick={() => setOpen(false)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:637** **Button** Create — disabled={!draft.start || !draft.end}; onClick={() => { addTravelRequest({ purpose: draft.purpose.trim() || 'Travel request', start: draft.start, end: draft.end, type: draft.type, estimatedCost: Number(draft.estimatedCost) || 0, project: draft.pro — UNVERIFIED
- **src/screens/app/travel/panes.tsx:761** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => setView(item.id)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:781** **input**  — type="date"; onChange={(event) => setFrom(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:790** **input**  — type="date"; onChange={(event) => setTo(event.target.value)} — UNVERIFIED
- **src/screens/app/travel/panes.tsx:798** **button** {days} d — onClick={() => { setTo(new Date().toISOString().slice(0, 10)) setFrom(new Date(Date.now() - days * 86_400_000).toISOString().slice(0, 10)) }} — UNVERIFIED

### src/screens/app/travel/settings.tsx

- **src/screens/app/travel/settings.tsx:51** **select** <option value="warn">Warn (flag, don&apos;t block)</option> <option value="block">Block (stop submission)</option> — onChange={(event) => set('enforcement', event.target.value as TeSettings['enforcement'])} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:62** **input**  — onChange={(event) => set('receiptRequiredAbove', event.target.value)}; placeholder="disabled" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:73** **input**  — type="checkbox"; onChange={(event) => set('flagDuplicates', event.target.checked)} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:87** **input**  — onChange={(event) => set('categoryLimits', { ...draft.categoryLimits, [category]: event.target.value }) }; placeholder="no limit" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:100** **Button** Save policy — disabled={!dirty}; onClick={() => updateTeSettings(draft)} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:120** **input**  — type="checkbox"; onChange={(event) => setDraft((prev) => ({ ...prev, enabled: event.target.checked }))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:130** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, address: event.target.value }))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:138** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ receiptEmail: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:164** **input**  — type="number"; onChange={(event) => setDraft((prev) => ({ ...prev, advancePercent: Number(event.target.value) }))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:175** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, approvalAbove: event.target.value }))}; placeholder="always" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:185** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, perDiem: event.target.value }))}; placeholder="not set" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:195** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ travel: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:244** **input**  — type="checkbox"; onChange={(input) => setDraft((prev) => ({ ...prev, [event.id]: input.target.checked }))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:256** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ travelNotifications: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:284** **input**  — onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === level.id ? { ...item, label: event.target.value } : item))) } — UNVERIFIED
- **src/screens/app/travel/settings.tsx:291** **input**  — onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === level.id ? { ...item, threshold: event.target.value } : item)), ) }; placeholder="always" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:302** **button** ✕ — onClick={() => setDraft((prev) => prev.filter((item) => item.id !== level.id))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:314** **Button** + Add level — onClick={() => setDraft((prev) => [...prev, { id: crypto.randomUUID(), label: 'New level', threshold: '' }])} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:320** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ approvalLevels: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:342** **input**  — type="number"; onChange={(event) => setDraft(Number(event.target.value))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:351** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ slaDays: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:373** **input**  — onChange={(event) => setDraft((prev) => ({ ...prev, [category]: event.target.value }))}; placeholder="unmapped" — UNVERIFIED
- **src/screens/app/travel/settings.tsx:383** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ glAccounts: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:410** **input**  — onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === field.id ? { ...item, label: event.target.value } : item)), ) } — UNVERIFIED
- **src/screens/app/travel/settings.tsx:419** **select** {['text', 'number', 'date', 'select'].map((type) => ( <option key={type}>{type}</option> ))} — onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === field.id ? { ...item, type: event.target.value } : item)), ) } — UNVERIFIED
- **src/screens/app/travel/settings.tsx:433** **input**  — type="checkbox"; onChange={(event) => setDraft((prev) => prev.map((item) => (item.id === field.id ? { ...item, required: event.target.checked } : item)), ) } — UNVERIFIED
- **src/screens/app/travel/settings.tsx:445** **button** ✕ — onClick={() => setDraft((prev) => prev.filter((item) => item.id !== field.id))} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:462** **Button** + Add field — onClick={() => setDraft((prev) => [...prev, { id: crypto.randomUUID(), label: 'New field', type: 'text', required: false }]) } — UNVERIFIED
- **src/screens/app/travel/settings.tsx:470** **Button** Save — disabled={!dirty}; onClick={() => updateTeSettings({ customFields: draft })} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:633** **Button** <Icon name="sparkles" size={15} /> Open Copilot — onClick={() => document.querySelector<HTMLButtonElement>('[aria-label="Open AI Copilot"]')?.click()} — UNVERIFIED
- **src/screens/app/travel/settings.tsx:648** **button** <Icon name={item.icon} size={16} /> {item.label} — onClick={() => setParams({ tab: 'settings', s: item.id })} — UNVERIFIED

### src/screens/app/workspace.tsx

- **src/screens/app/workspace.tsx:90** **button** Mark all read — onClick={markAllNotificationsRead} — UNVERIFIED
- **src/screens/app/workspace.tsx:103** **button** <span aria-hidden className="mt-0.5 text-accent"> 🕐 </span> <span className="min-w-0 flex-1"> <span className="block text-[14px] font-semibold">{item.title}</s — onClick={() => markNotificationRead(item.id)} — UNVERIFIED
- **src/screens/app/workspace.tsx:196** **Button** Sign out — onClick={signOut} — UNVERIFIED

### src/screens/studio/AgentStudio.tsx

- **src/screens/studio/AgentStudio.tsx:40** **button** <span aria-hidden className="w-4 text-center text-[13px]"> {item.icon} </span> {item.label} — onClick={() => onSelect(item.label)} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:66** **button** <span className="flex items-center gap-3"> <span aria-hidden className="w-4 text-center text-[13px] text-fg-muted"> {group.id === 'library' ? '▤' : group.id === — onClick={() => setCollapsed((prev) => prev.includes(group.id) ? prev.filter((id) => id !== group.id) : [...prev, group.id], ) } — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:90** **button** <span aria-hidden className="w-4 text-center text-[13px]"> {item.icon} </span> <span className="flex-1">{item.label}</span> {item.badge && <span className="text — onClick={() => onSelect(item.label)} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:188** **button** ‹ — onClick={() => setCursor(new Date(year, month - 1, 1))} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:196** **button** › — onClick={() => setCursor(new Date(year, month + 1, 1))} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:267** **input**  — placeholder="Search agents, tables, logs..." — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:275** **button** {theme === 'dark' ? '☀' : '☾'} — onClick={toggleTheme} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:282** **button** ? — onClick={() => setHelp(true)} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:300** **form** <textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} rows={2} aria-label="Describe an agent" placeholder={studioPromptPlaceholder} class — onSubmit={(event) => { event.preventDefault() const text = prompt.trim() if (!text) return // Building an agent is a run like any other, so it lands in Runs // and in the audit trail rather than vanishing. rec — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:314** **textarea**  — onChange={(event) => setPrompt(event.target.value)}; placeholder={studioPromptPlaceholder} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:323** **input**  — type="file"; onChange={(event) => { const picked = Array.from(event.target.files ?? []).map((file) => file.name) setAttachments((prev) => [...prev, ...picked]) event.target.value = '' }} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:334** **button** + — type="button"; onClick={() => fileInput.current?.click()} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:342** **button** 📎 — type="button"; onClick={() => fileInput.current?.click()} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:351** **button** ↑ — type="submit"; disabled={!prompt.trim()} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:368** **button** × — type="button"; onClick={() => setAttachments((prev) => prev.filter((_, at) => at !== index))} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:399** **button** × — onClick={() => setHelp(false)} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:421** **button** <span aria-hidden className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-accent/20 text-[15px] text-accent" > {item.icon} </span> <span className=" — onClick={() => setCategory(item)} — UNVERIFIED
- **src/screens/studio/AgentStudio.tsx:464** **button** <span aria-hidden className={`grid h-8 w-8 shrink-0 place-items-center rounded-lg text-[13px] ${workflow.tone}`}> {workflow.icon} </span> <span className="trunc — onClick={() => setPrompt(workflow.name)} — UNVERIFIED

